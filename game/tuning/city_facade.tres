; The facade look the city actually loads, shared by every tile.
;
; `tools/generated_scene_import.gd` maps the ETL's `city_facade` material name to
; this path and only this path, so this file *is* the switch: it currently names
; `city_facade_clean.gdshader`, and `city_facade_warm.tres` beside it names the
; measured `city_facade.gdshader`. Both read the same `TEXCOORD_0` payload, so
; swapping is a copy and a reimport — never a rebuild.
;
; Here rather than as shader defaults because CLAUDE.md hard rule 4 makes tuning
; data, not constants — and beside `golden_hour.tres` because the sun these are
; judged under is tuned in the same directory. Loaded as **one** resource for all
; 66 tiles: retuning the city is this file.
;
; `floor_height_m` is still the measured 2.8 m and stays that way whichever
; shader is loaded — the massing is real and the floor rhythm should be. What the
; clean look changes is the *scale everything else is drawn at*: glazing spans a
; group of storeys and a bay is 9 m, because the measured 2.4 m column pitch is
; about four pixels across from a moving car and reads as noise rather than as
; architecture. See the shader header.
;
; ⚠️ **Every facade element is currently switched off, and the city is massing
; plus flat colour.** User's call on 2026-08-06, driven: with `Q27` fixed the
; per-building colour finally carries the city on its own, and the window grid was
; competing with it rather than adding to it. Deliberately parked rather than
; deleted — "until we come up with some new art idea to try on" — so the shader
; keeps all of it and this file is the only thing holding it back.
;
; **Seven parameters do it, and it takes all seven.** `solid_share = 1.0` is the
; gate: `glazed = step(solid_share, …)` in the shader, so at 1.0 no building is
; ever glazed and the curtain-wall, fin and punched types stop drawing. That
; alone leaves the street-level furniture behind — measured, 86% of the frame
; still differed — because shopfronts, cornices, floor lines and mullions are
; drawn outside that gate, so each is zeroed too.
;
; To bring the windows back, take the seven values from `542cac3` — nothing else
; in the file moved, and none of it needs a rebuild:
;
;   solid_share 0.27 · glass_ratio 0.44 · shopfront_share 0.55 · accent_share 0.11
;   cornice_darkness 0.28 · floor_line_darkness 0.11 · mullion_darkness 0.12
;
; The dozens of uniforms those seven make inert — `bay_width_m`, `glass_colour`,
; `pane_bow`, the fresnel terms — are left at their tuned values on purpose. They
; are the state a future art idea starts from, not dead weight to be cleaned up.
;
; ⚠️ **`base_wash` is 0.0 because the palette lives in the city config**, which
; is where the shader's header says it belongs. The `materials:` table in
; `etl/config/cities/hong_kong.yaml` carries the city's level — every colour in
; it was re-authored 19 L* darker once the anchor probe measured that as what the
; white city needed — so the wash has nothing left to trial. That leaves
; `base_colour` inert, kept at its tuned value on the same rule as the window
; uniforms above.
;
; ⚠️ Those colours used to sit inline on the height bands, and `Q34` moved them:
; the field's position asserted that material is a function of height, which 0.9%
; of measured facade lightness does not support. Nothing about the level changed.
;
; `value_jitter` 0.35 is the small half of that change and is bounded on both
; sides. Below it a block reads as one mass; above roughly 0.4 the darkest draws
; stop reading as buildings differing and start reading as a few of them being
; broken. It is not the lever that fixed the white city — at the 0.6 ceiling it
; moved the frame 0.4 L*, against 10.3 for the bands.
[gd_resource type="ShaderMaterial" load_steps=2 format=3]

[ext_resource type="Shader" path="res://assets/shaders/city_facade_clean.gdshader" id="1_facade"]

[resource]
resource_name = "city_facade"
shader = ExtResource("1_facade")
shader_parameter/base_colour = Color(0.79, 0.805, 0.82, 1)
shader_parameter/base_wash = 0.0
shader_parameter/value_jitter = 0.35
shader_parameter/warm_cool_jitter = 0.07
shader_parameter/floor_height_m = 2.8
shader_parameter/band_storeys = 1.0
shader_parameter/glass_ratio = 0.0
shader_parameter/bay_width_m = 5.5
shader_parameter/mullion_ratio = 0.26
shader_parameter/mullion_darkness = 0.0
shader_parameter/podium_height_m = 12.0
shader_parameter/shopfront_base_m = 0.7
shader_parameter/shopfront_top_m = 4.6
shader_parameter/shopfront_share = 0.0
shader_parameter/shopfront_pier_ratio = 0.46
shader_parameter/cornice_thickness_m = 0.5
shader_parameter/cornice_darkness = 0.0
shader_parameter/solid_share = 1.0
shader_parameter/fin_share = 0.2
shader_parameter/curtain_share = 0.16
shader_parameter/punched_share = 0.22
shader_parameter/fin_pier_ratio = 0.36
shader_parameter/punched_pier_ratio = 0.52
shader_parameter/bay_scale_min = 0.72
shader_parameter/bay_scale_max = 1.45
shader_parameter/two_storey_share = 0.22
shader_parameter/accent_share = 0.0
shader_parameter/accent_course_ratio = 0.0
shader_parameter/glass_colour = Color(0.21, 0.27, 0.33, 1)
shader_parameter/glass_tint_b = Color(0.13, 0.22, 0.21, 1)
shader_parameter/glass_tint_c = Color(0.33, 0.43, 0.51, 1)
shader_parameter/sky_zenith = Color(0.24, 0.47, 0.78, 1)
shader_parameter/sky_horizon = Color(0.85, 0.91, 0.96, 1)
shader_parameter/ground_reflection = Color(0.32, 0.33, 0.35, 1)
shader_parameter/pane_bow = 0.3
shader_parameter/pane_jitter = 0.1
shader_parameter/glass_face_on = 0.18
shader_parameter/sky_reflection = Color(0.62, 0.78, 0.92, 1)
shader_parameter/glass_reflect = 0.58
shader_parameter/fresnel_power = 4.0
shader_parameter/glass_roughness = 0.12
shader_parameter/accent_a = Color(0.85, 0.19, 0.16, 1)
shader_parameter/accent_b = Color(0.11, 0.45, 0.72, 1)
shader_parameter/accent_c = Color(0.95, 0.62, 0.09, 1)
shader_parameter/accent_opacity = 0.8
shader_parameter/wall_sky_tint = 0.18
shader_parameter/floor_line_darkness = 0.0
shader_parameter/grounding_height_m = 3.0
shader_parameter/grounding_darkness = 0.26
shader_parameter/wall_roughness = 0.82
shader_parameter/wall_normal_max = 0.28
shader_parameter/roof_darkness = 0.2
shader_parameter/fade_start_m = 140.0
shader_parameter/fade_end_m = 244.0
shader_parameter/lit_window_colour = Color(1, 0.85, 0.55, 1)
shader_parameter/lit_window_share = 0.0
shader_parameter/emission_strength = 0.0

# Progress

**Live state only.** What is in flight, what is measured, what is at risk. Update it whenever a task
changes status, a number is re-measured, or a question opens or closes.

- **Why something is the way it is** → `DECISIONS.md`, keyed by the `Q` or task ID.
- **What happened when** → git.

Last updated: 2026-08-20

---

## Current status

**Phase 0 and Phase 1 are complete. Phase 2 is two hardware-blocked tasks from its gate.**

The ETL turns six government map sheets and one road geodatabase into a drivable Wan Chai in **3.0 s
from an empty `out/`**: 65 vertex-coloured tiles at two LOD tiers, 797 road edges over 615 nodes with
217 turn restrictions, one 35k-triangle road surface with trimesh collision, and 29 fare nodes. Godot
streams it, the car drives it, and it exports to a web PCK.

**Shipped in Phase 2, all five reviewed and passed:** `CityStreamer` (`P2-1`), `RoadGraph` (`P2-2`),
the queried start line (`P2-3`), the chase camera and building collision (`P2-5`), and the off-grade
carriageway on its real structure (`P2-7`). **Left:** `P2-4` (`InputRouter` across three input paths)
and `P2-6` (the perf pass) — both blocked on `P0-3b`, which needs a signing identity and the two
floor handsets. **Nothing on the critical path is blocked on software.**

**Phase 3 runs `B2` → `B1` → `B3` → `B4`** — the Hong Kong driving *experience* is finished and
tested before any taxi gameplay element exists, so test players judge the scene on its own. `B2`
carries the player taxi (`P3-11`), and `P3-9a` then puts that build in front of ≥3 HK drivers over a
web link. Order, deps and acceptance are in `PLAN.md`; the reorder and what it costs are in
`DECISIONS.md`.

**`B2` is complete and `P3-9a` is in progress — the build is re-cut, the drivers are not booked.**
✅ **`P3-12` and `P3-13` both landed 2026-08-19, after the first artefact was cut**: the road draws
its own markings — lane dividers, centre lines, kerbside double yellows and a bus-lane edge —
procedurally over the lane coordinate `P1-4` shipped, with **no texture** and no extra draw call,
and the kerbside yellow is sourced from `NSR` rather than invented (`Q53`, `Q54`). **The call was
made on 2026-08-19: both go in front of the drivers**, so the artefact was re-exported on top of
them and of `P3-13`'s kerb-rail thinning — **39.16 MiB PCK**, `tools/check.sh` green, smoke-driven
with 0 shader errors. Arrows and box junctions are held on *content* — 🔴 **which `Q57` corrected on
2026-08-20: both are published, and the hold is a cost call now** (`Q53`, `Q57`).
✅ **`Q56`, 2026-08-20, moved the kerbside yellow again, and the artefact was re-cut on it the same
day.** `P3-13` refused `NSR`'s `VEHICLE_TYPE = 5` because the specification names no class; a second
dataset — TD's Traffic Aids Drawings v2 — shows a painted line on **93.9%** of those metres, so
`painted_vehicle_types` is `[1, 5]` and the region publishes **33,385 m over 722 edge sides**,
**+28.1%**. **PCK 39.17 MiB**, `check.sh` green, every owed grader re-run.
⚠️ **That re-cut reached the macOS export and not the web one, and the gap stood for a day.** Found
2026-08-20: `build/web/` was a 19 Aug export and the itch zip an **18 Aug** one predating `P3-12`, so
the packaged distributable had **no road markings at all**. Both are current now — **76.19 MiB over
the wire**. ⚠️ **Nothing in the repo builds that zip, so re-pack it by hand on every re-export**;
second instance, mechanism in `DECISIONS.md` `P3-9a`.
✅ **The same source also holds box junctions as polygons** — 20 in region — so `Q53`'s "no source"
for that one is now false too, and is not yet acted on.
🔴 **`Q57`, 2026-08-20, swept the whole estate a third time and retired three more "no source"
claims. Nothing is built and nothing is fetched.** The arrows are published
(`DTAD_RD_MARK_SYM_PT`, **1,365 symbols** in region with an `RM` code and a bearing) and so is the
road text (`DTAD_RD_MARK_ANNO`, 274). **The carriageway width is published** — iB1000
`CartoTransLine` `RM`, *"Road margin"*, on sheets downloaded since `P3-7a` — reading p50 **9.84 m**
against a `width_m` that takes two values, 6.4 m and 9.6 m. **The tram tracks are published**
(`CartoTransLine` `TW`), against a config comment that said no dataset marks them. ⚠️ **The
mechanism is the same one `Q54` and `Q56` were each corrected for**: a fact measured against Road
Network v2, then generalised to the estate. ⚠️ Follow-on work is named and unassigned in `Q57`.
`Q26` closed on candidate `C`, and `P3-7a` closed at what was judged with its remaining survey
riders gated on `P3-9a` reopening the look (`DECISIONS.md` `P3-7a`). Everything in `B2` is shipped
and awaiting review; nothing is unstarted before the gate. The web build was re-exported
**2026-08-20** on top of `Q56` and verified end to end — see the `P3-9a` row for what was checked
and for the `SharedArrayBuffer` hosting constraint, which is the only open item.

**The premise is measured rather than assumed.** `Q8` closed when the user drove the real city: an
HK-like map is a fun enough gimmick on its own. That retires the founding risk and replaces it with a
narrower one — *gimmick* is the user's own word, and a gimmick carries a first session. "Novelty does
not survive the first session" is the live entry in the register, and it is `P3-*`'s to answer.

**Two things are knowingly missing from the world.** The **elevated network is closed to driving** —
`nearest_edge` refuses all 60 off-grade edges (`Q13`, reopened deliberately in Phase 4) — and
**nothing in the authenticity table is built**: no traffic, no trams, no neon (`P3-3`, `P3-4`,
`P3-8`). The ground shipped with `P3-10` and is solid, so leaving the carriageway now puts the car on
the pavement instead of through it.

### Task board

| ID | Task | Status | Notes |
|---|---|---|---|
| `P0-1` | Source data granularity | ✅ Done | 6 sheets, glTF per sheet ~44 MB. Buildings are scriptable; closed `Q2`, `Q3`, `Q5`. |
| `P0-2` | ⚠️ Z-value spike | ✅ Done | No Z, but `ELEVATION` encodes the level. Region holds. Closed `Q1`. |
| `P0-3` | Godot project scaffold | ✅ Done | Godot 4.7.1. macOS / web / Android export verified. |
| `P0-3b` | Mobile device build verification | ⬜ Not started | Needs a signing identity and the two floor handsets. Blocks `P2-4`'s and `P2-6`'s reviews. |
| `P0-4` | ETL scaffold | ✅ Done | `pipeline/` + `hong_kong.yaml`. Found the ~304 m datum trap. |
| `P0-5` | Grey-box fun test | ⚠️ Passed, conditional | Handling accepted; fun verdict deferred to `Q8`, now closed. |
| `P0-5a` | └ Vehicle controller approach | ✅ Done | Custom raycast on `RigidBody3D`. `VehicleBody3D` measured and rejected. |
| `P0-5b/c/d` | └ Circuit, camera, drive test | ✅ Done | Circuit from JSON, spring-arm camera, driven. |
| `P1-1` | Source fetching | ✅ Done | Sheets derived from the published index, not listed. Idempotent. |
| `P1-2` | Building meshes | ✅ Done | 65 tiles; 989k → 434k → 222k triangles. Verified in-engine. |
| `P1-2t` | └ Terrain evaluation | ⚠️ Superseded | Judged unaffordable at 267 MB — but 224 MB of that was the JPEG. Replaced by `P3-10`, which ships no texture. |
| `P1-3` | Road graph | ✅ Done | 797 edges, 615 nodes, 217 turns, 96.3% connected. Closed `Q9`, `Q11`, `Q12`. |
| `P1-4` | Road surface mesh | ✅ Done | One mesh, one draw call, kerbs, trimesh collision. All 393 single-level junctions covered. Opened `Q13`. Two driver-reported defects fixed: kerbs buried in a neighbour's carriageway, and the hull pinching the road at a bend. |
| `P1-5` | Fare nodes | ✅ Done | 29 nodes (14 stands, 6 cross-harbour; 15 PUDO) from 793 territory-wide points. Opened `Q14`, `Q15`. |
| `P1-6` | Export and manifest | ✅ Done | `city.json` + one-command pipeline; byte-reproducible. Opened `Q16`. |
| `P1-7` | Godot import | ✅ Done | **Phase 1 gate passed.** Georeferenced to 1 cm, checked in-engine. |
| `P2-1` | `CityStreamer` | ✅ Done — review passed | Threaded load/unload by published `aabb`. Draw calls 70 → 53; the review dropped the exact-weld tier, closing `Q16`. |
| `P2-2` | `RoadGraph` + debug overlay | ✅ Done — review passed | One parse per scene; p99 **45 µs** against a 1 ms budget. Refuses all 60 off-grade edges (`Q13`). Found the carriageway-width contract gap. |
| `P2-3` | Vehicle on real geometry | ✅ Done — review passed | `RoadSpawn` resolves the start line through `RoadGraph`; the hand-written transform is deleted. Verdict: *"car seems ok"*. ✅ **`Q52` added the clearance guard 2026-08-18** — the pose says what it is standing in, and `verify_spawn.gd` refuses a start line a car does not fit in. |
| `P2-4` | `InputRouter` | ⬜ Not started | Touch, gamepad and keyboard → one action set. Its review needs `P0-3b`'s handset. |
| `P2-5` | Chase camera | ✅ Done — review passed | Unblocked by shipping building collision. No shape-cast needed. Opened `Q19`, `Q20`. |
| `P2-7` | Off-grade carriageway on its structure | ✅ Done — review passed | Deck heights sampled from `INFRASTRUCTURE`. \|error\| p90 **4.13 m → 0.095 m** against a 0.50 m criterion, graded against the shipped tiles by a tool sharing no code with the pipeline. Closed `Q20` and `Q23`, largely closed `Q13`, opened `Q21` and `Q22`. Nothing became drivable. |
| `P2-6` | Performance pass to budget | ⬜ Not started | **Phase 2 gate.** Runs last because it measures the geometry that ships. Needs `P0-3b`. |
| `P3-11` / `P3-10` / `P3-7` / `P3-7a` / `P3-6` | Build `B2` — "it reads as HK" — **runs 1st** | 🟡 In progress | Taxi first (it is in every later screenshot), then ground (`Q18`), then the window shader, then the hero buildings. `P3-6` shipped its first two heroes 2026-08-12; `P3-7a` closed as shipped 2026-08-17 with its remaining riders gated on `P3-9a`. **Nothing remains before the `P3-9a` gate — it is open.** |
| `P3-11` | └ Player taxi model | 🟡 **Awaiting review** | Generated by `tools/make_vehicle.py`; **1,180 triangles** in scene across two `.glb`s (604 body, 4 × 144 tyres). Untextured. Chassis taken from the scene, not chosen. Latest shots in `build/driver/taxi_*`. ⚠️ The rocker strip and red valance are unjudged from the play camera, which never shows the flank. |
| `P3-11c` | &nbsp;&nbsp;└ Body shader — clearcoat over a sky gradient | 🟡 **Awaiting review** | `vehicle_body.gdshader` + `vehicle_body.tres`, bound through `generated_scene_import.gd`'s name channel; surface marker in `UV.y`. Triangles, draw calls and materials all unchanged. Glazing, lenses and paint each take the same three-band reflection (zenith / horizon / ground) chosen by the reflected ray's elevation — **the gradient, not its strength, is what made it read**: two flat-colour tunings were judged wrong in opposite directions before it went in. Graded at the `taxi` cameras: glazing `L*` 0.52 → 9.28 with `L*` sd **1.25 → 7.69**, silver trim `C*` 10.00 → 7.59, red paint `C*` 79.06 → **71.63** — a priced trade, on a dial, and still 9× the frame median. **0 pixels moved outside the car**. ✅ Ice-blue roof fixed — half of it was an authored `b* -3.56`, not ambient. 🔴 **The red tail lens is not fixed**; both obvious fixes are closed decisions and it wants a call, not tuning — ✅ `P3-11d` is that call. Sun glint added on top: `sun_glint.gd` feeds the shader the scene's real `DirectionalLight3D`, so glazing catches the sun at some headings (`L*` +6.03) and not others (+0.01) — the one term that answers steering, since the sky gradient is yaw-blind. Shots in `build/driver/p311c/` and `build/driver/sweep_tuned/`. ✅ **The engine end is now held**: `tools/verify_vehicle.gd` (2026-08-18) requires every surface of the body to render with `vehicle_body.tres` through the name channel, and requires `sun_glint.gd` to be fed that same resource — dropping the material name leaves the car on a `StandardMaterial3D` and the tool names it. ⚠️ It still cannot see a *frame*: headless has no rasteriser and Godot exits `0` on a shader that fails to compile, so the look is a render and a `grep -i "shader error"` (`P3-11c`) |
| `P3-11d` | &nbsp;&nbsp;└ Lamp circuits — brake, reverse, indicators | 🟡 **Awaiting review** | `UV.x` — reserved and zero since `P3-11c` — now carries a switched circuit per lens, and `scripts/vehicle/vehicle_lamps.gd` closes them from the car's own state. **+12 triangles** (the high-level brake lamp), no extra draw call, no extra material. ✅ **Closes `P3-11c`'s open 🔴**: the red tail lens goes `L* 2.29 → 72.72` under braking and is invisible coasting, which is what an unlit brake lamp should be — nothing recoloured, nothing put back behind the cluster. ⚠️ The circuits are **`instance uniform`**, not material state: `vehicle_body.tres` is shared by the whole roster and a plain uniform would brake every taxi at once. ⚠️ `lamp_emission` trades bloom against redness and nothing else past 1.2 — ACES desaturates the clipped red channel, so 2.3 is a *white* lamp with a red glow. Shipped at **1.6** (`C*` 44 at the core). Indicators need **0.5 s of held lock** as well as a 0.35 threshold, or the tail strobes through every correction. ⚠️ An **eighth** palette colour, `DEEP_RED`, whose darkness is the feature. Shots in `build/driver/final_*`. ✅ **The circuit now has to reach the shader**: `verify_vehicle.gd` requires `lamp_lit` and `lamp_front` to appear in the *renderer's own* instance-uniform list for the body mesh — an unmatched name is a no-op rather than an error, and demoting either to a plain `uniform` fails it too — and requires the imported mesh's `UV.x` to stay inside the 8 circuits those two vectors carry, on lens vertices only |
| `P3-11e` | &nbsp;&nbsp;└ Front lamps — side lamps in shade, main beams under cover or at night | 🟡 **Awaiting review** | Wires the two lens pairs `P3-11d` deliberately left on `CIRCUIT_NONE`. **No new geometry** — `taxi_body.glb` is the same 604 triangles and the same 47,608 bytes; only `UV.x` moved. `lamp_lit` was a full `vec4`, so circuits 5–8 land in a second `instance uniform`, `lamp_front`, and the shader's existing out-of-range guard — shipped by `P3-11d` against exactly this — now bounds at `CIRCUIT_COUNT` and selects between the two vectors with a masked slot, so neither operand can go out of range if a Mobile driver flattens the branch. The lamps answer to the **light, not the driver**: `Lighting` is a three-rung ladder (`SUN` / `SHADOW` / `DARK`) decided by two raycasts — sky blocked overhead is `DARK`, sun blocked with sky open is `SHADOW` — and cover is tested first, or a car in an underpass would stop at side lamps. Graded at the HKCEC deck: the lit lens clips **L\* 100.00** against a mean **38.74** for the cluster box, and the same lenses in open sun peak **96.71** with no bloom — reflective, not emissive. ⚠️ **Two of the three triggers have nothing to fire on today**: `Q21` records level −1 as undrivable, so the tunnel case is served by the overhead probe and proven under the HKCEC deck instead; and night is `Q26`'s unbuilt rig, so `read_rig` is a hook rather than a measured path. ⚠️ The sun probe is bounded by **where collision exists**, not where shadow does — only the finest tile tier ships a collider and `streaming.tres` puts that at 250 m, so at `golden_hour`'s 30° sun a caster **taller than ~115 m** throws its far shadow beyond the ray, and that shadow reads as sunlit. **The lenses glow; two `SpotLight3D`s on the same ladder are what actually light the road** — full on `DARK`, 0.3 of the energy *and* the reach on `SHADOW`, hidden in daylight. Shipped without it first and the user caught it: blazing lamps over a black road under the deck. A/B'd on the same seeded run by stashing the scene, **`prims` and `draws` are bit-identical** with one cone and with none, and the second cone moved neither — a shadowless spot adds neither, and shadows stay off because `ART_DESIGN.md` grants the mobile tier none. ⚠️ `spot_angle` is Godot's *half* angle: 34 authored a 68° flood from 0.3 m that lit the sky and the bumper and read as a puddle. ⚠️ **The cone must not reach above horizontal** — 7° down against an 11° half-angle put the *top* of the beam 4° **up**, which lit a dome climbing the screen and read, correctly, as a light shining upward; **14° down against 13° puts the top edge 1° below horizontal**, so every ray lands on tarmac. The half-angle is otherwise set by pool separation — lamps 0.58 m off centre merge from `0.58/tan(angle)` on, so 22 read as one blob and 15 as one lobe with a notch. ⚠️ **The beams take the lighter of the held and current state where the lenses take the held one**, so a beam can never paint sunlit tarmac through `light_hold_s` — which the single-spot version did on every exit from the deck. Shots in `build/driver/lamps_*`, `beam*`, `twin*`, `aim14*` and `exit*`. ✅ **The aim is now checked rather than commented**: `verify_vehicle.gd` requires every beam to be authored dark and its cone to sit entirely below horizontal, and the 7°-down/11°-half version this shipped first fails it at **4.00° up** — the number that was found by looking at a frame. ⚠️ It grades the scene, never a frame, and the night rung still has no rig to fire on |
| `P3-10` | └ Ground surface | 🟡 **Awaiting review** | Terrain is a tiled class: **+87,649 triangles** at LOD0, no texture, **no extra draw call**, **+4.56 MB of PCK**, and it **collides**. `ground_sink_m: 0.20` sized by `tools/ground_clearance.py`. Reads as ground on the flat core; **buries the carriageway on hill streets** — `Q24`. `Q29` fixed what the review found: the ground was smooth-shaded. Shots in `build/driver/q29*`. |
| `P3-7` | └ Window-band shader, and its `TEXCOORD_0` payload | 🟡 **Awaiting review** | One commit across both sides; `city.json` schema 4 → 5. Storey height **2.8 m measured**, not guessed. **+4.01 MB of PCK**, **zero triangles moved**, no extra draw call. Shots in `build/driver/p37*`. Its payload now draws by default through `city_facade_clean` (`A‴`, 2026-08-09); the measured-pitch look itself remains the `city_facade_warm.tres` alternative — see `Q26`. |
| `P3-7a` | └ Survey-driven façade variation | ✅ **Closed as shipped 2026-08-17** — `W1`/`W2`/`W3` accepted and shipped; `Q47` route decided, **iB1000 ingestion landed 2026-08-10, tower↔block join landed 2026-08-11**; **`R4` graded 2026-08-11 — failed its pre-fixed bar; route re-called the same day: data-only pack**. ⚠️ **The remainder — `R1`, `R2`, `R3`, `R4`'s pack, `W4`, two-tone walls and the paid ground-band batch — is neither cancelled nor scheduled**, gated on `P3-9a` reopening `Q26` (`DECISIONS.md` `P3-7a`) | `W1` punched-as-glass and `W2` per-building pane colour landed and were **accepted by the user on the `A‴` frames 2026-08-09** ("much more acceptable now"); `city_facade.tres` shipped the look by default from 2026-08-09 until 2026-08-16, when the user moved the default back to `C` and `A‴` went to `city_facade_elements.tres` unchanged (`Q26`). `Q30` bar held (+1.16/+0.78/−0.05 against `A`'s +2.28/+1.43/−0.02). User drive-test of the shipped default (2026-08-09) returned five sightings of **invented fenestration on windowless stock** (HKCEC service base/tunnel, plant boxes, a footbridge lift tower, sportsground walls) and named storefronts as under-invested where the player actually looks. Measured against the survey: grammar refused on 35% of 2,213 buildings, rising to 88% of stock under 4 m, and `blank` committed only 10× region-wide, so refusal is the only scalable "quiet" signal. **`W3` landed 2026-08-10 (`Q46`)**: five `quiet_*` tunables quiet the hash on the 772 grammar-refused buildings — shopfront needs committed grammar, refused stock draws punched-with-heavy-piers only at a higher solid share with muted panes, committed stock untouched by construction. `Q30` bar held (**+1.10/+0.74/−0.04**, below `A‴`'s street/kerb cost), parked `C` byte-identity re-proved with the quiet values authored; **accepted in scope 2026-08-10** on a `survey_debug`-tinted drive test (the tint, a committed dev tool in `city_facade_clean.gdshader`, paints facades by survey state) — refused stock reads quiet, and the residual wrong windows sit **overwhelmingly on committed stock**: `Q41`'s tower-biased majority verdicts painting ground bands, a population `W4` cannot absorb (`Q47`). **iB1000 ingestion landed 2026-08-10** (`P3-7a`, three commits): `gdb.py` decodes polygon-Z in both WKB dialects (GDAL hands back the wkb25D high bit, found by the same-GDAL round-trip test), the `topography` tiled source fetches the six sheets (260.0 MB measured, idempotent; the host's incomplete TLS chain completed by a committed intermediate — `extra_cas`, see `DATA_SOURCES.md`), and `buildings.podium_blocks` reads the `Building` layer behind the `podiums:` config block — the acceptance test reproduces `Q47`'s verified counts from inside the pipeline (1,595 = 1,220 `T` / 280 `P` / 76 `OS` / 19 `TS`, levels 100% filled on every `T`/`P`). **Tower↔block join landed 2026-08-11** (`P3-7a`, four commits): the contract argued against `ARCHITECTURE.md` in its own commit (floors stay the codec — data-metres convert against the packed pitch at pack time, provenance ETL-side, ladder `authored > data > survey > hash`, no bump), plan-polygon predicates hoisted to `geometry.py`, and the `podiums` stage (between `fetch` and `buildings`) stitches the clipped per-sheet pieces (1,595 → 1,480 logical; a cut *clips* — zero whole-block duplicates) and joins them to the meshes spatially, depth-gated at 0.3 m: 458/1,134 towers meet a `P` block by true polygon overlap (228 exact level meets; the record's 668/54.8%/247 is the probe's bounding-box frame, reproduced as such by the acceptance test), and **310/1,385 stems carry a data boundary** in `podiums.json` (291 certain; HKCEC 52.1 m from its own `P` block, Times Square at exactly 75.6 mPD). **`R4` graded 2026-08-11, and failed its pre-fixed bar** (`tools/podium_error.py` — bar fixed at `3949e41` before the grader existed, an instrument sharing no measurement with the join): \|error\| p50 **10.76 m** against a **2.80 m** criterion (n=121; p90 12.87 against 7.0), signed median **−10.76 m** — a systematic three-to-four-storey undershoot, only 2.5% within half a pitch — and the survey sees no podium on **40.4%** of decided certain rows against ≤33.3%; coverage passed (69.8% ≥ 60%). **Zero adjudicated corrections**: the miss is `Q43`'s predicate gap measured — `podium_floors` reads the treatment band, the `P` roof bounds the massing (worst miss: the 75.6 mPD flagship's ~16-storey podium mall read, correctly by the prompt, as 2 floors of treatment). The reconciled pitch validated in passing: 124/310 commit, p50 **3.28 m** against `Q42`'s 3.32. Closed `Q42`'s podium slice as a fail; the survey-complement half of `Q47`'s route was reopened and **re-called the same day: data-only** — Spearman ρ = **0.076** between the survey's converted metres and the data boundary (no per-building signal), so the best fitted correction (\|err\| p50 2.46 in-sample) is dominated by a constant the shader already ships (`podium_height_m` 12.0: \|err\| p50 2.97 on the same pool); survey podium metres never pack. Next: `W4` authored override table (exceptions; smaller now — HKCEC's podium extent comes from its `P` block) → riders `emphasis` → storey pitch → `balconies` → podium pack (data-only: `podiums.json` rows against the packed pitch, after `R2` so the round-trip bound holds; survey metres never pack), each against a pre-fixed bar → ground-band storefront batch (its prompt now owes the boundary predicate — *where the tower grid starts, in floors*) (design now, pay after `P3-9a`). Gated behind `W3`: two-tone walls (`x` bit budget on paper first). Scouts run 2026-08-10: the street-facing bit's blocker is now measured — a survey-quadrant payload corrupts 0.6–0.9% of triangles across 23% of buildings through `collapse`'s representative pick, so the hold stands (`PLAN.md`); iB1000 carries podium blocks and utility structures as first-class feature classes — `Q47`'s third route, **verified to `DATA_SOURCES.md` grade 2026-08-10** (levels 100% filled on every building and podium block in region, sub-metre registration, 260 MB keyless download), and `Q47`'s two named routes are measured in its record — **the route call was made 2026-08-10**: iB1000 primary (`P`-block metres, data > survey-inferred), `R4` complement graded against the 668-tower overlap before packing, ground-band batch unchanged in scope (`Q47`). The `C` look remains the reducibility baseline, and since 2026-08-16 it is also the shipped default — baseline and default are now the same file. See `PLAN.md` `P3-7a`. ⚠️ **`P3-6` (2026-08-12) excluded HKCEC from the tiles, so `W4`'s canonical HKCEC entry is moot — do not author one; `W4` shrinks to the remaining stragglers.** |
| `P3-6` | └ Hero buildings via `landmarks.json` | 🟡 **Awaiting review** — 2 of ~5 shipped | **HKCEC (Phase 2 wing + link) and Central Plaza shipped 2026-08-12**, generated by `tools/make_landmark.py` (3,484 + 300 triangles, vertex-coloured, `Q33`-checked palette; HKCEC's wing stations, deck, piers and under-deck infill are measured from the source mesh and the road graph — Expo Drive still passes beneath the halls, now through solid-walled portals rather than a pier field, after kerb-level review found the deck reading as a building on stilts). **Re-treated and re-sliced the same day against Expo Drive East street views** (user judged the source mesh closer to the photos): values un-inverted (pale `panel_pale` hull, dark ribbon glazing, darker `roof_grey` sweep), stations re-measured asymmetric at 8 m bands (banana plan, 14 m prow, east flank rolling to ~20 m, hull edges their own measurement) and faired with a σ = 1-band Gaussian so the wing reads as one smooth sweep in crisp facets; ribbons then moved from wall-height fractions to constant storey-line elevations after they fanned where the east roll pinches the wall. Contract landed as drafted plus `excluded_bounds`: `landmarks:` in the city config, stems as keys, `export.py` assembles `landmarks.json`, `city.json` **schema 6 → 7** both sides one commit, `buildings.json` v3 records exclusions. `replaced = 2` meshes exactly; set-equality checked both directions at export, tile-interior probe in new `verify_landmarks.gd` (in `check.sh`). **Amended 2026-08-12: HKCEC now ships as its own repainted source mesh** — the generator rounds had converged on the source by measurement and the user judged the source superior, so `pipeline/landmarks.py` (new stage) extracts, slices at the ribbon elevations, vertex-repaints via the config's `source_paint` block and ships it as *generated* data (never committed — licensing hard rule 7); the authored `hkcec.glb` is deleted, the generator keeps Central Plaza, `city.json` **7 → 8** / `landmarks.json` **1 → 2** (`landmark_assets`, per-entry `triangle_budget`), **99,577 triangles** against a 120k pin, PCK **33.85 → 38.67 MB**. Review round 2026-08-13 ("texture doesn't follow the accurate shape") rebuilt the classifier: roof/soffit crease-grown so the sweeps stay grey to their rolled edges, and ribbon strips vetoed strip-wise against the building's own aerial texture (individualised `A0`, consulted at build time, never shipped) by local vertical contrast. Hopewell, Times Square (massing only — `Q42`), govt slabs remain. Shots in `build/driver/p36*` and `build/driver/hkcec_ref3_*`. See `DECISIONS.md` `P3-6`. |
| `P3-12` | Road markings on the ribbon | 🟡 **Awaiting review** | Lane dividers (3 m dash on 6 m), a continuous centre line where `TRAVEL_DIRECTION` says two-way, kerbside double yellows and a bus-lane edge on the 13 published `BUS_ONLY_LANE` edges — all procedural, **no texture**, `mesh_contract.gd` unmoved. `roads.glb` gained `TEXCOORD_1` (packed class/lanes/direction/bus/tram, plus the edge's drawn length) and the material name `road_markings`; `city.json` **9 → 10**, both sides one commit. **+41,344 B of PCK** measured from two exports with one variable changed, against 279,532 B raw — the pack compresses it 86% because `x` has 17 distinct values region-wide. **Zero triangles moved, no extra draw call, no extra material.** ⚠️ **The payload shipped as a per-vertex distance-to-nearer-end first and that was wrong**: it is a V with its kink at the midpoint and a strip interpolates linearly, so on the **204 of 797 edges with two stations** both ends read zero and the whole street faded out. A unit test on a two-station fixture caught it before any frame did; the channel carries the edge's *length* instead and the consumer takes `min(V, length − V)` per fragment. ✅ **The junction fade is priced, not chosen**: the cap overlap was measured per arm end at p90 **1.17 m**, p99 **3.62 m**, worst **4.21 m** across 203 of 1,398 ends — reproducing `ARCHITECTURE.md`'s published 210 from a different direction — and 6 m clears all of them. It shipped at 9 m first, which cost **48 more edges** their markings for no defect covered (169 → 121 of 797 bare). ⚠️ **The 6,051 m² overlap is still there**, only no longer drawn over; a box junction re-exposes it. 🔴 **The kerbside double yellow is invented, and the claim that it could not be sourced was wrong** — the geodatabase's `NSR` layer carries **579 features / 44,220 m** in region, kerb-referenced (median 2.92 m off the centreline) with `TIME_ZONE` separating all-day from posted-hours, i.e. double from single. Painted on every kerb it asserts no-stopping over ~3x the restricted length, over **607 parking bays** and over its own **14 taxi stands**. Held on `draw_double_yellow` until `NSR` is ingested; the join needs a linear-referencing stage (only 78% of NSR parts agree on their own side) and the payload cannot express a V-range while 204 of 797 edges carry two stations. ✅ **Opposed pairs closed the offside half**: `_hide_buried_kerbs`'s verdict is published as `offside_kerb`, and the six merged dual carriageways (564 m, Lockhart/Fleming/Tonnochy) now carry a published centre line — ⚠️ a U-lane is `2*half_width/lanes` (5.12 m), not `lane_width_m`, and the first version put the join off the road. ⚠️ Moving the surface to a `ShaderMaterial` took it off `generated_scene_import.gd`'s `vertex_color_is_srgb` branch, so the shader carries `vertex_srgb_to_linear` by hand (`Q27`); verified against the `street` baseline — asphalt unchanged, only markings new. `verify_road_surface.gd` gained the material check, a full codec scan and both importer traps (`meshes/light_baking`, 16-bit compression), neither guarded before because `roads.glb` had never carried UV2. Shots in `build/driver/markings_*`. See `DECISIONS.md` `Q53`. |
| `P3-13` | Kerbside no-stopping from `NSR` | ✅ **Done 2026-08-19** | The one shipped marking that was invented, sourced. **Closes `Q54` at 240% → 4%.** `pipeline/kerbside.py` linear-references `NSR` onto the finished graph — 1 m sampling, nearest **level-0** edge, side from the offset's sign, deduped per cell, gaps under 3 m bridged, runs under 5 m dropped — and `roadgraph.json` publishes **26,065 m over 650 edge sides** at **schema 4** (20,414 double / 5,651 single, from 372 of 579 features). ⚠️ **`VEHICLE_TYPE` is the field `Q54` never read**: only `1` "all motor vehicles" is paint (33,074 m of 44,220 m); `2`/`3`/`4` are signs (2,822 m) and `5` "Others" names no class (**8,323 m refused, metres recorded**). `TIME_ZONE` is double-vs-single outright. **Level 0 only, measured**: for 7% of samples the nearest edge of any level is elevated, and their own street is a median 4.0 m away. **Payload:** two 2-bit codec fields for the kind (max code **2,097,151**) and `COLOR_0.a` per **rail** for the extent — a channel `_rgba` had been broadcasting as an unread 255. **Graded on the shipped mesh** by `tools/kerbside_error.py`: **16,511 m painted against 16,726 m reachable — 4% gross error** (over 200 m, missed 415 m), where a line on every kerb over-paints 40,127 m. **Cost, one variable changed:** road mesh 34,924 → **39,176** vertices (+12.2%) from 1,179 inserted stations; **PCK 38.88 → 39.16 MiB (+287 KiB, +0.72%)**. ⚠️ **New finding: 2,909 m of real restriction is on a kerb the drawn city does not have** — the 1.6x widening paves over the strip between two parallel ribbons (Gloucester Road, Lung Wo Road, Harbour Drive) and `MARKING_OFFSIDE_KERB` correctly says there is no kerb there. No shader change reaches it. ⚠️ **`Q54`'s taxi-stand claim was an *under*statement**: **9 of 14** stands sit inside a genuine restriction. **Known residue:** 183 m drawn as the wrong *kind* where a side carries both (the codec says one kind per side); 6 new inward-folding triangles, 1.84 m², from the inserted stations at tight corners. ✅ **The kerb's share of that cost was dead weight, and is gone 2026-08-19**: the extent is written on the carriageway strip alone — every kerb vertex carries `COLOR_0.a` 255 — but `_add_kerb_stations` inserted onto `edge.points`, which all four kerb strips are drawn from too. `_rail_stations` now draws the kerbs from the stations they need and no more: **44,142 → 39,176 vertices (−11.2%)**, the whole saving on the kerb — **4,966 of the 6,884 vertices the insertion cost it, 72%** — with the carriageway (11,200) and caps (3,390) untouched. ⚠️ The kerb does not return to its pre-`P3-13` 22,668: the 1,918 that stay are what the load-bearing stations cost. **PCK 39.34 → 39.16 MiB.** ⚠️ **The estimate this was priced against was wrong, and the guard is what found it**: 6,884 kerb vertices were called dead by stubbing the insertion out, and **390 of the 1,179 stations are not** — they carry a kerb height the chord across them does not have, by up to **87 mm**, so stubbing would have stepped kerbs off their own carriageway on a one-trimesh collider. 252 kept for height, 126 as buried-kerb run ends, 12 as ribbon ends; **789 were free and went**, at a worst measured **0.099 mm** against a 0.1 mm bar. Mechanism and the full measurement in `DECISIONS.md` `Q54`. Checked against a rebuild of the old mesh: every surviving kerb position is in it bar one, **30 µm** away — one float32 ULP — and the Hennessy Road frame differs by 35 pixels of 2,073,600, all at the vanishing point. ⚠️ The 6 inward folds are **not** touched (10 / 2.37 m² either side), so they are on the carriageway strip and stay open. All owed graders reproduce their baselines — `narrowing.py` (no edge cleared to 1.3x, e207/e595 lost), `clearance_reconcile.py --sweep` (24 vs 26), `deck_error`, `overhang`, `ground_clearance`, `carriageway_occupancy` (26, failing as recorded). ⚠️ **Amended 2026-08-20 by `Q56`**: the `VEHICLE_TYPE = 5` refusal above was wrong — those 8,323 m are painted, `painted_vehicle_types` is `[1, 5]`, and the published figures in this row are superseded by **33,385 m over 722 sides** (19,764 / 13,621, 530 of 579 features), 1,113 stations, 39,128 vertices, PCK 39.17 MiB, `kerbside_error` 3% of 22,451 m reachable, wrong-kind residue 359 m, 11 inward folds at 2.41 m². The `2`/`3`/`4` "are signs" reasoning was also wrong and the refusal now rests on the codec instead. |
| `P3-9a` | Recognition round 0 — the city, before the game | 🟡 **In progress — artefact re-cut 2026-08-20, waiting on drivers** | ≥3 HK drivers on the `B2` web build. No HUD, nothing to do. Asks *do they know where they are*, and *did they keep driving anyway*. ✅ **Re-cut 2026-08-20 on `Q56`, and this time the itch zip was re-packed with it** — the 19 Aug cut below had reached `build/web/` only, and the zip was still an 18 Aug one predating `P3-12`. Sizes in the Bundle-size metric. Provenance proven before exporting: a full pipeline re-run left all 273 generated files byte-identical bar `city.json`'s `generated_utc`. `tools/check.sh` exit 0, `ruff` clean, **1103** tests pass, `project.godot` and `export_presets.cfg` unmoved. Smoke-driven with **0 `SHADER ERROR`**, and loaded in Chrome over the real COOP/COEP headers with **0 console errors or warnings** — 6 `[log]` lines, `multi-threaded` build config, `road surface: 32233 triangles`, `start line: f_004 on edge 651 (EXPO DRIVE)`. All four render tells re-confirmed under the HKCEC deck at 27.5 km/h under braking (`build/driver/recut_brakefwd`). ⚠️ **The visible signature of `Q56` is a kind change, not just extent**: HENNESSY ROAD (`e745`, `e245`) drew a *double* yellow on the 19 Aug cut and now draws a *single*, which is `Q56`'s audit row "published double, drawing single: 1,055 m → 66 m" landing on screen. The 2026-08-19 cut it replaces: `tools/check.sh` green, 39.16 MiB PCK, smoke-driven with 0 `SHADER ERROR`, kerbside yellows and lane lines confirmed on the Hennessy Road viewpoint. The 2026-08-18 cut before that: `tools/check.sh` green, web exported on top of `P3-11e` (the 2026-08-17 02:05 build predated it and would have shown no headlamps), smoke-driven with **0 `SHADER ERROR`** and loaded in Chrome with **0 console errors or warnings** — the taxi renders with its paint chromatic, the sky gradient in the glazing, tail lamps lit under the HKCEC deck and the high-level brake lamp lit under braking, so none of `P3-11c`/`d`/`e`'s silent-unbind paths has fired. ⚠️ **Hosting is the one thing left and it is not free of constraints**: `variant/thread_support=true`, so the build needs `SharedArrayBuffer` and therefore **COOP + COEP response headers** — which **rules out GitHub Pages**, and is the entire reason `tools/serve_web.py` exists rather than `python -m http.server`. ⚠️ **76.19 MiB over the wire** (re-measured 2026-08-20); warn testers off mobile data. |
| `P3-1a` / `P3-5a` | Build `B1` — "one fare" — **runs 2nd** | ⬜ Not started | Fare state machine + deliberately ugly HUD. |
| `P3-3` / `P3-4` / `P3-8` / `P3-2a` | Build `B3` — "the streets are alive" — **runs 3rd** | ⬜ Not started | Traffic, trams, bus lanes, and near-miss scoring moved up from `B4`. ✅ **`Q51` landed 2026-08-18, ahead of the build**: the graph now publishes a clear corridor width per station and `RoadGraph.is_routable` is the predicate traffic routes on, so `P3-3` starts with the blocked edges expressed as data rather than lurking. ✅ **The 21-against-26 gap is reconciled (2026-08-19)** — plan cell size, verified against brute-forced ground truth, and `tools/clearance_reconcile.py` ratchets both counts (`Q51`). ✅ **It found a live defect — `clearance.py`'s 1 m along-edge spacing aliased walls — and the fix landed 2026-08-19**: `ALONG_M` is `CELL_M` now, `is_routable` no longer sends traffic down `e636` HARBOUR ROAD, and the published starved set is **24** rather than 21. So `P3-3` starts on a predicate that is a lower bound at the plan cell rather than one that had a known hole in it. Still owed by `P3-3` itself: adjacency (the graph stores no `from`/`to`), the 217 published turn restrictions nothing reads, and whether the player's headlamp slot is reserved (`BeamBudget`). |
| `P3-2b` / `P3-1b` / `P3-5b` | Build `B4` — "it's a game" — **runs 4th** | ⬜ Not started | Style chain, cross-harbour fares, full HUD. |
| `P3-9` | Authenticity test round 1 | ⬜ Not started | **Phase 3 gate.** ≥3 HK drivers on a handset, arrow disabled. Different drivers from `P3-9a` — that cohort has learnt the map. |
| `P4-*` | The elevated network | ⬜ Not started | Post-slice, but broken down: the data is measured and shipping collision half-opened the network by accident. Reverses `P2-2`'s refusal; closes `Q15`. |

Legend: ⬜ not started · 🟡 in progress · ✅ done · ⚠️ conditional · ❌ blocked

---

## Open questions

Each row is the live status. **The claim, the evidence and what is refused are in `DECISIONS.md`**
under the same ID.

| # | Question | Impact | Owner | Status |
|---|---|---|---|---|
| `Q6` | Does the region need Central for the circuit to feel complete? | Scope | after `P3-9` | 🟡 Deferred |
| `Q13` | Nothing ramps between elevation levels in the source's attributes | 23.3% of carriageway area unreachable | `P2-2` → `P4-1` | 🟢 Largely answered — all 36 mixed nodes are ramps, median step 0.04 m. What remains is 5 tunnel portals and a stub |
| `Q14` | Taxi stands carry operating-time restrictions that `P1-5` discards | A part-time cross-harbour stand is modelled as full-time | `P3-1` | 🟡 Open, deferred — a schema bump plus a parser whenever the fare loop needs it |
| `Q15` | Fare nodes snap by plan distance only, because the published points are 2D | A stand under a flyover cannot prefer the street below. No Wan Chai node is affected | `P4-2` | 🟡 Open — not reachable with this source |
| `Q19` | **26 drivable level-0 edges keep less than one lane clear** | Invisible walls on legal carriageway, and the player can still drive into one | the width question | 🟡 **Half answered.** The **routing** half closed 2026-08-18 as `Q51`: the bundle publishes a clear width per station, `RoadGraph` exposes `is_routable`, and `P3-3`'s traffic will not be sent down a blocked edge — so this is no longer `P3-3`'s to own. The **geometry** half stands: 26 level-0 edges hold under 3.2 m clear, worst `e233` **0.00 m** (WAN CHAI INTERCHANGE), and ✅ **Narrowing refused 2026-08-18 on the whole population**, not the sample of two it rested on before, and ✅ **re-swept 2026-08-19 on `Q51`'s corrected population where it hardened**: `tools/narrowing.py` sweeps every factor `GAME_DESIGN.md` allows and **no edge clears the bar at any of them**, while **two** are lost to the clipping — `e595` THOMSON ROAD and now `e207` CANAL ROAD EAST. ⚠️ **The blockage splits by class** — 9 `INFRASTRUCTURE` (7 of them the WAN CHAI INTERCHANGE), 14 `BUILDING`, 1 `LANDMARK` — and the two halves do not share a fix. 🔴 **Corrected 2026-08-19: the `BUILDING` half is not the mild half.** This row used to record every near-total blockage as `INFRASTRUCTURE`; at the finer spacing **three of the five 0.00 m edges are `BUILDING`** (`e627`, `e636`, `e740`). The coarse walk was disproportionately missing building walls, which are thin in plan along the street they face. ⚠️ **19 of the 24 are under the car's own 1.8 m**, so these are edges a player cannot enter, not ones they thread awkwardly. ⚠️ The gate is per-edge, not the region share. 🟡 Shares still unreconciled against the recorded 5.17% (total **3.693%**), most likely because `P3-6` took HKCEC out of the tiles after that figure was taken. ⚠️ **The 26 is resolution-bound**: the grader reads **26 / 18 / 9** starved edges at a 1.00 / 0.50 / 0.25 m occupier bin (`Q51`, 2026-08-19), so the headline is very nearly a function of one constant ✅ **The occupiers are named, 2026-08-20.** The corridor listing was capped at eight rows and now prints all 26 with the blocker and the **shape** of the blockage. The two halves separate cleanly and not as this row assumed: every `BUILDING` failure starves **1-3 m** of its edge and clears again, while `e233` starves **42 m unbroken** and `e55` 25 m. 🔴 **The building half is not a frontage** — 12 of the 14 are **under 20 m long** (median 10.4 m against the graph's 47.3 m) and not one structure failure is. Driven and shot at `e314` and `e627` (`build/driver/q19/`): **the ribbon is drawn wider than the gap it runs through**. ⚠️ **The mechanism is upstream of every width argument here**: `DATA_SOURCES.md` already records that lane counts exist in no layer of the source, and that is *why* this half fails — 720 edges at 6.4 m and 77 at 9.6 m, all invented from the speed-limit table, so the `authored` column is invented too and narrowing cannot reach it. ✅ **An answer does follow, and `Q57` found it 2026-08-20: it is the other dataset, and it is already on disk.** iB1000 `CartoTransLine` `RM` — *"Road margin"* in LandsD's own dictionary — is the carriageway edge, **56,286 segments** over the six sheets `P3-7a` already downloads. Probed at 9,822 stations on 701 centrelines it reads p25 **7.02 m**, p50 **9.84 m**, p75 **15.36 m**, against a `width_m` of exactly two values. HyD **Pavement Polygon** (**1,714** polygons over the envelope, with an `LVL` mirroring `ELEVATION`) is the second, unfetched. ⚠️ **The probe over-reads** — the perpendicular escapes through junction mouths and crosses dual carriageways — so it is evidence a width exists, not a width to ship. ✅ **The real cross-section landed 2026-08-20 as `tools/carriageway_margin.py`**, `Q57`'s follow-on row 2: it measures **near-side overhang** (`drawn half-width − nearest published edge`) rather than a width, which drops the ray that the junction mouth and the dual carriageway corrupt, and reads **12,502 stations on 709 of 737 level-0 edges at 92.3% coverage** against both publishers. The ribbon **does** cross the published kerb — p50 **+1.59 m**, p90 **+3.24 m**, 75.0% of stations past it, 63.0% past it by a metre — and ✅ the headline is **not** a function of its own constants, moving only p50 1.69 → 1.50 m across a 10–25 m ray cap. 🔴 **But it fails to predict this row's own population, and that is the finding.** On edges **under 20 m**, where 12 of the 14 `BUILDING` failures live, overhang reads p50 **−0.36 m** — *inside* the published kerb — and 35.3% past a metre against 66.2% on edges over 47.3 m. Either the junction artefact still dominates (a ray crossing an open mouth finds a farther kerb, which reads as less overhang) or **the building half is not a width defect at all**. ⚠️ Junction exclusion cannot separate them: below twice the exclusion radius an edge has no non-junction station, so "short edge" and "junction" are the same population and the `< 20 m` band empties outright. 🔴 **And asked about this row's own named edges it mostly cannot answer**: `e636` HARBOUR ROAD and `e595` THOMSON ROAD return **no station at all**, `e627` GREAT GEORGE STREET and `e314` LEIGHTON ROAD one each — with a nearest kerb 8-10 m away on a 7-11 m street, i.e. the ray leaving through the junction mouth. Only `e207` CANAL ROAD EAST (52 m) behaves. **So "ship the published width" is not the sequel this row assumed**, and whatever closes the building half must work on stubs an 8 m ray cannot cross. Still unassigned; the interchange half is `Q22`/Phase 4's. ⚠️ **`e702` EXPO DRIVE CENTRAL is pipeline-only** — grader 3.41 m against the pipeline's 0.75 m, the largest disagreement in `clearance_reconcile`, and `Q51`'s to own |
| `Q51` | **Traffic is never *sent* down an edge under one lane clear; the player is never *stopped*** | Whether `P3-3` can start at all, and whether a blocked road still names itself under the player | `P3-3` | ✅ **Closed 2026-08-18.** `etl/pipeline/clearance.py` measures a cross-section every `ALONG_M` along each edge and publishes `carriageway[].clear_width_m` into `city.json` (**schema 8 → 9**, `roadgraph.json` untouched); `RoadGraph` gains `is_passable` / `is_routable` / `impassable_edge_ids` and `nearest_edge` is **deliberately unchanged**, so a player stuck against a wall still gets the road name and lane centre. ✅ **The 21-against-26 gap is reconciled 2026-08-19, and it is 6 + 1 rather than "five"** — the grader condemns `e99` `e132` `e207` `e222` `e636` `e781` that the pipeline clears, and the pipeline condemns `e702` that the grader clears. **The mechanism is plan cell size**, not the junction treatment this entry first proposed: deck heights agree to 0.000 m median and dropping every trimmed station moves the grader 26 → 26, while brute-forcing `e132` from 109 M surface samples reproduces **0.98 m in 1.0 m cells and 4.00 m in 0.25 m cells** — both published figures exactly. `tools/clearance_reconcile.py` ratchets the counts. ✅ **The reconciliation found a live defect, it was the pipeline's, and the call was taken 2026-08-19**: at `ALONG_M = 1.0` the walk stepped over walls it never sampled. **`ALONG_M` is `CELL_M` (0.5 m) now**, so the published width is a lower bound at that cell. `e636` HARBOUR ROAD reads **0.00 m** and the two agree about it; published count **21 → 24**, disagreements **7 → 4**, three edges to agreement and none the other way, no schema bump. Cost **3.8 s → 4.4 s** and **482 MB → 789 MB**. ⚠️ A bound at `CELL_M` and no finer — a 0.25 m walk reads 25 and was refused at **1.088 GB** peak RSS. 🔴 **Still open: 9,779 triangles a run** hit `MAX_SUBDIVISIONS` and block by a box at full height range, counted and warned — deliberately not folded into the same change. The arguments are in `DECISIONS.md` `Q51` |
| `Q52` | **Can the spawn put the player somewhere a car does not fit?** | A start line inside a wall, on a road the graph calls legal | `P2-3` | ✅ **Closed 2026-08-18.** `RoadSpawn.Pose` carries `clear_width_m` at the station the car stands on and `blocked()` reads it against the lane; `drive_harness.gd` warns and places anyway, `tools/verify_spawn.gd` **fails**. ⚠️ **The bar is where the car stands, not `is_passable`** — the router's whole-edge minimum would condemn a start line standing in clear road because a wall stands elsewhere on the same edge; `edge_passable` carries that answer as a note instead. ⚠️ **The resolution is the segment, and Wan Chai's run to 154.6 m** — on the 201 two-station edges the two readings coincide exactly, and they differ on 31 of 2959 segments, always toward blocked. ⚠️ **Fires on nothing in the shipped bundle**, so it is proven on five built start lines and by breaking the guard five ways, each break caught by exactly one case — including a plain `minf` over the two stations, which reads `-1.00 m` and goes quiet on the very segment with the wall in it (**562 of 2959 segments have one unmeasured end**, the start line's own among them) |
| `Q53` | **Can the road draw its own markings without a texture, and how much of a real road is in the data?** | The largest continuous surface in every frame, and what `P3-9a` grades | `P3-12` | ✅ **Closed 2026-08-19.** Lane dividers, centre lines, kerbside double yellows and a bus-lane edge draw procedurally over `P1-4`'s lane coordinate; `roads.glb` gained a `TEXCOORD_1` payload (`city.json` **9 → 10**) because U alone cannot tell a kerb from a lane boundary, and a cap's `(0, 0)` is an in-range value rather than a sentinel. **+41,344 B of PCK**, no triangle moved, no draw call added. ⚠️ **Arrows, road text and box junctions are refused on data, not cost** — no Road Network v2 layer carries a lane attribute at all, and the only imagery showing real markings is the orthophoto `Q18`/`Q36` refused at a **1.6×** width misregistration. The box-junction *mechanism* is solved (a world-space hatch is cap-immune); the content has no source. 🔴 **The "no source" half is false for all three, and `Q57` finished retiring it 2026-08-20** — Traffic Aids Drawings v2 publishes 20 yellow box junctions as polygons (`Q56`), **1,365 road-marking symbols** carrying an `RM` code and a bearing (`RM1017` ahead ×353, `RM1019` left ×179, `RM1021` right ×92), and **274 road-text annotations** with `TextString` and `Angle`. All three held markings are published; the hold is a **cost and registration** call now, and the record says so. 🔴 **The kerbside double yellow is invented, and the "no source" claim was wrong — that is now `Q54`.** ⚠️ The 6,051 m² cap overlap is faded around, **not fixed** |
| `Q54` | **The kerbside yellow is invented, and the layer that would source it was read past** | The game asserted a legal no-stopping restriction on 3.4x the kerb that carries one | `P3-13` | ✅ **Closed 2026-08-19 by `P3-13`.** `roadgraph.json` publishes 26,065 m of restriction at schema 4 and the extent reaches the shader in `COLOR_0.a`; `tools/kerbside_error.py` grades the shipped mesh at **16,511 m painted against 16,726 m reachable — 4%**, against 40,127 m of over-paint for a line on every kerb. ⚠️ **Four of this row's original numbers were wrong** and the corrections are in `DECISIONS.md`: 33,074 m paintable not 44,220 m; 3.4x not 3x; median offset 2.76 m not 2.92 m; and **9 of 14 taxi stands are genuinely restricted**, so the harm was on the other five. ⚠️ **One obstacle nobody saw**: 2,909 m lands on a kerb the widening paved over, and no shader change reaches it. ✅ **Not** blocked by the widening that killed the orthophoto (`Q18`/`Q36`) — the geometry is read for side and extent, never for position. ⚠️ **A fifth number was wrong and `Q56` fixed it 2026-08-20**: the 8,323 m of `VEHICLE_TYPE = 5` this record refused is painted, and the published restriction is now **33,385 m over 722 sides** |
| `Q21` | Should level −1 carriageway be drawn at all? | 11.6% of carriageway area with no viewer, solid since collision shipped | Phase 4 | 🟡 Open — heights cannot be improved (a tunnel is a void), and 11 of 30 ends are clipped at the region boundary |
| `Q22` | **10.2% of off-grade carriageway hangs past its structure** | Cosmetic until Phase 4, when a wheel leaving the deck finds air | Phase 4 | 🟡 Open — no width rule reaches the rest |
| `Q24` | **The road is a plane and the ground is not** | Solid geometry in legal road, on top of `Q19`'s occupancy | `roads.py` | 🟢 Half closed — along the road, area proud **3.289% → 1.898%**. Across the road is untouched and is `Q19`'s |
| `Q30` | **The shipped façade palette is not the one `ART_DESIGN.md` authorises** | One building in four is more saturated than the direction sanctions | `hong_kong.yaml`, `ART_DESIGN.md` | 🔴 Open, re-measured against `Q37`'s survey by `tools/facade_chroma.py` — **20.1% → 26.4% over `C*` 20**. ⚠️ `facade_hue.strength` cannot fix it — amplifying chroma widens the spread faster than it moves the middle. ✅ **Re-owned 2026-08-17 by `Q26`'s closure on `C`, not answered by it** — with the elements off, the excess is **entirely the base palette** (`facade_hue` × height-band weights, baked into `COLOR_0` by the ETL and present under all three looks), so the question is now smaller and better posed than when it was parked on the look. 🟢 `Q43` moves it without being aimed at it: matte openings take no sky reflection, so `A″` gives back 39% of `A`'s chroma cost at `street` and 60% at `kerb` while keeping every opening |
| `Q31` | **The city's value range has an empty middle** | Half a street frame carrying no information — the frame the player occupies all game | `P3-9a` | 🟡 Open, **re-diagnosed**. The statistic now has a tool and tests (`frame_stats.py` band shares) and reproduces the published figures. Cause is **`adjustment_contrast = 1.14`, never ablated** — 1.14 → 1.00 takes `kerb` 51.3% → **0.9%** and `taxi` t01.20 28.9% → 0.8%; the fill is upstream of that curve and is re-crushed by it. 🔴 ⚠️ **The band share is the wrong acceptance test** — it moves a flat surface across a threshold (mass sd 0.79 → 0.85), so grade within-mass variation, which only a sky-visibility bake can supply. Nothing shipped. ✅ **Audited against `Q26`'s mis-aimed-camera hazard and every row reproduces exactly** — `kerb` 51.3/2.7, `street` 13.0/25.4, `skyline` 0.0/3.8, `infra` 0.0/39.6, the last re-shot in three runs that agreed byte for byte. ⚠️ The `build/driver/art_*` shots are **not** the frames the table was computed from; the audit-viewpoint table regenerates them, `build/driver/` is not provenance. ✅ **No longer blocked by `Q26`** — the candidates separate identically at 1.14 and 1.00, so the curve is not what the looks are told apart by; it still waits on the acceptance test |
| `Q35` | **A per-building material draw gives a salt-and-pepper skyline** | Two adjacent blocks can land 13 reflectance points apart, where real blocks share cladding | `buildings.py`, `hong_kong.yaml` | 🔴 Open. Candidates: a spatial hash on position, a block join from an external dataset, or accepting it. ⚠️ **Grade it from the street, not the skyline** |
| `Q38` | **`exposure_anchor` is baked into `COLOR_0` at build time**, so a time-of-day change is a full rebuild | Night is planned, and this is the lever it needs | night mode | 🟡 Open, deliberately not fixed. The fix is cheap and known; it costs `_check_exposure` and its test, both shipped to close a real defect |
| `Q39` | **`wall_sky_tint` is uniform**, so a canyon wall takes a rooftop parapet's sky bounce | Overstates sky bounce in exactly the frames `Q31` reports as broken | `Q31` | 🟡 Open — free once a sky-visibility term exists, and nothing before that. ⚠️ Do not lower it globally. ✅ `Q31` now argues that term is **structural**: no uniform lever can vary a flat surface, so the bake is the only route for either question |
| `Q42` | **The reader answers seven questions nobody consumes** | `storey_count`, podium, balconies, emphasis and signage are collected on every paid call the full-sheet run will make anyway — free survey data if validated, a second `schema_version` bump avoided if packed into `TEXCOORD_1` now | `P3-7a` | 🟡 Open — from the 40 paid validation responses: storey pitch median **3.32 m/floor** against `Q40`'s independent **3.38 m** (two instruments, shared photography only), `emphasis` 25/25 and coherent with grammar, `band_period_floors` 0/25 (never commits). 🔴 `signage` reads real trademarks (SHUI ON GROUP, REVENUE TOWER, YMCA, FWD) — identification and placement only, never rendered text. Nothing rider-shaped consumed yet, but ✅ **`TEXCOORD_1.y` ships reserved at a documented bit layout** (schema 6), so each field now owes exactly its own validation — no further schema bump. The podium slice was graded 2026-08-11 against the joined boundaries and **failed its pre-fixed bar**; the pitch prescription validated in passing (p50 3.28 m) — see `Q47` |
| `Q47` | **A committed verdict is right about the tower, wrong about the ground band** | The tinted drive test put the residual wrong windows overwhelmingly on committed stock — `Q41`'s tower-biased verdicts paint the band the player drives past | `P3-7a` | 🟡 **Route decided 2026-08-10** — the sequence: iB1000 `P`-block metres where a committed tower meets one (54.8% of towers; precedence **data > survey-inferred**, extending the `authored > survey > hash` ladder), `R4`'s floors→metres conversion on the complement with the 668-tower iB1000 overlap as its validation set (`Q42`'s bar discharged by a join, not hand labels), and the ground-band batch unchanged in its shopfront-only scope. Closes when the shipped boundary is graded; iB1000 ingestion ✅ **landed 2026-08-10**, tower↔block join ✅ **landed 2026-08-11** (`P3-7a` — `podiums` stage: 1,595 pieces stitched to 1,480 logical blocks, 310/1,385 stems carry a data boundary with `mechanism: "data"` provenance in `podiums.json`; contract argued against `ARCHITECTURE.md` in its own commit — floors stay the codec, metres convert against the packed pitch at pack time, ladder `authored > data > survey > hash`); ✅ **`R4` graded 2026-08-11 — failed the pre-fixed bar** (\|err\| p50 10.76 m against 2.8, no-podium rate 40.4% against ≤33.3%, coverage 69.8% passed, zero adjudicated corrections — the miss is `Q43`'s predicate gap, measured at −10.76 m median); **re-called the same day: data-only** (Spearman ρ = 0.076 between converted floors and the data boundary — no per-building signal, so a constant the shader already ships beats the best fitted correction), `R4` packs `podiums.json` rows alone in its slot after `R2`, and the ground-band batch owes the boundary predicate |
| `Q48` | **A contrast ratio measures banding where an `L*` profile could not** | `P3-6`'s photo veto is a measurement class `Q40` never tested — it needs no contiguous chart, so Probe 3 does not reach it, and band presence is a tone claim, so mode 1 does not either. If it survives, it is the discriminator `Q47`'s committed stock needs and the one reader field (`band_period_floors`, 0/25) that never commits | `P3-7a` | 🟡 **Open as a candidate — nothing built, nothing scheduled** (recorded 2026-08-13). ⚠️ Mode 4 *does* reach it — periods appear in blank walls, which is the target population — so the one-way veto (photo may only *remove* bands; a refusal keeps the procedural verdict) is a constraint on any use, not an implementation detail. 🔴 Evidence is **n = 1**, graded by its author's eye against reference photographs — `Q40`'s retained overclaim is the precedent for not treating that as validation. Splits in two: **pitch is not a new question** (four instruments already measure it — `P3-7`'s 2.77 m *ships* as `floor_height_m = 2.8`, against `Q40` 3.38 / `Q42` 3.32 / `Q47` 3.28, unreconciled — so a mechanical read is the third grader the queued storey-pitch rider is short of, not a `W`-item); **presence is the new claim**. No new source is needed, and ✅ **the sheets are on disk after all — corrected 2026-08-17**: all six sit in `etl/sources/individualised/` (5.7 GB, fetched 2026-08-06, verified intact), so a region run costs **no download**. ⚠️ The retracted "one of six, 5.86 GB hand re-download" priced the `fetch.source_dir` tree, `etl/sources/hong_kong/individualised/`, while all three survey tools default `--zip-dir` one level up. The tool still owes its own sample lattice, a pre-fixed bar on more than one sheet, and `verify_tiles.gd`'s `uv2.y` range check |

| `Q49` | **A physically honest handbrake spins the car, and the design target is anti-physical** (raycast car) | `GAME_DESIGN.md` asks for a drift that slides a lot and scrubs little; a locked rear axle trades one for the other, so the two cannot both be had from the real mechanism | `handling.tres`, `B4` | 🟡 **Superseded in mechanism by `Q50` (2026-08-18)** — the car is Godot's `VehicleBody3D` now, so the friction ellipse is gone and `handbrake_lock` with it. ✅ The `B4` conclusion stands and is now the *only* route left. Everything below is the raycast car's record, kept because `Q50` is measured against it: ✅ The friction ellipse itself is settled and shipped: lateral and longitudinal now spend one budget, so braking through a corner costs cornering grip, and a 4 s full-lock corner **holds 62.26 km/h where it used to gain to 81.64**. 🔴 The handbrake is the unresolved half. Fully locked, peak slip hits **162.1°** — the same verdict as the **162.6°** `P0-5a` measured on the *rejected* `VehicleBody3D` and called a spin, though `P0-5a`'s figure predates `Q49`'s unification of the slip formula — and **lowering the dial made it worse** (0.05 → 0.30 gave 177.3° → 162.3°), because that axle's friction is the only thing resisting yaw. A 0.5 s tap span it too, so "make it a tap" is not the fix. Blending rolling against locked removes the spin but welds slide to scrub: the shipped 0.15 buys **16.0° slip at 2.39 m/s²** where the retired fudge held **57.8° at 1.96**. ✅ The route out is `B4`'s per-wheel angular velocity — a real driver sustains a slide on the throttle by spinning the driven rear wheels back up, which is unreachable without it. Swept with `tools/skidpad.sh --handbrake=`, now `--drift-grip=` |
| `Q50` | **The shipped car is Godot's `VehicleBody3D`, and `P0-5a` was right** | `P0-5a` rejected `VehicleWheel3D` because its friction is isotropic; the user instructed the switch anyway. The question is no longer whether to switch but what to do about the drift that cannot be tuned. | `handling.tres`, `B4` | ✅ **Closed as shipped 2026-08-18** — and it reproduced `P0-5a` rather than refuting it. 🔴 Three regressions, all structural: `Q49`'s friction ellipse is a circle again (corner yaw **−358.2°** against **−428.9°**, and a power-on corner accelerates once more); the drift window is **0.01–0.02 wide** with nothing in it at `drift_slip_threshold_deg`'s 14° (0.68 → 2.0°, **0.66 → 21.8° shipped**, 0.64 → 36.4°, 0.60 → 75.2°); and a 0.5 s handbrake **tap now does nothing at all** (1.9° peak, yaw and distance identical to `corner`, against the raycast car's 7.1°). ✅ Braking and coasting reproduce to within 0.5%, which is what proves the hand-written coast drag and collision response ported. ⚠️ `brake_force` re-seeded **2400 → 40**: Godot's `brake` is a different quantity and the old value stopped the car at **173 m/s²** over 1.0 m. `DECISIONS.md` `Q50` carries the full table |
| `Q55` | **The façade survey's filler guard reads greyness, and the placeholder panels are coloured** | `facade_lab.json` is the input every façade decision rests on — `Q30`'s chroma, `Q34′`'s ring weights, `Q45`'s panes | `tools/facade_survey.py`, survey | 🔴 **Open — measured 2026-08-20, not fixed.** `is_filler` rejects an exact `R == G == B` tie, and **97 atlases on 93 of the 2,213 buildings** carry a flat panel whose colour is not one; **92 of the 93 clear `vegetation_max`**, so the pipeline trusts them. Re-estimating with those texels rejected moves **43 past `Q33`'s 0.46 `Δab`**, 15 past 2.0, 5 past 5.0, worst **+54.69 `L*`**. ⚠️ **The panels are byte-identical duplicates** — one 4,584 B file on 21 buildings, one 1,761 B file on 29, two of them a *single* colour. ⚠️ **Two mechanisms**: bright filler wins `Q37`'s 65th-percentile cut, and dark filler wins by **mass** (44–96% of walls), which `Q37`'s brightness argument does not cover. ⚠️ **The error runs both ways** (+54.69 / −33.22, median +0.00) and is **severely skewed** — p50 share 0.94%, and the largest population (29 buildings) is inert at worst 0.82 `L*`. ✅ **The grey half is sound**: 2,429 of 3,203 atlases are caught, 1,982 at `#3c3c3c`. ⚠️ **93 is a floor** — detection needed one exact colour at ≥20% of an atlas. **Fix is per texel, never per atlas** (four 4096² photographs carry embedded fill), and re-publishing the table owes `ring_weights.py` and `facade_chroma.py`. `DECISIONS.md` `Q55` |
| `Q56` | **Is `VEHICLE_TYPE = 5` a painted line, and is there any other source for kerbside yellow?** | `Q54` refused 8,323 m on a reading of the spec that no re-reading could have corrected | `pipeline/kerbside.py` config, `tools/kerbside_source_audit.py` | ✅ **Closed 2026-08-20 by the change that opened it.** `painted_vehicle_types: [1, 5]`; the region publishes **33,385 m over 722 edge sides**, up 28.1% from 26,065 m over 650. Settled by a **second dataset** — Traffic Aids Drawings v2 `DTAD_RST_ZONE_LINE`, which draws the line as painted: **93.9% of code-5 metres carry one**, 96% of them single yellow, and only **24 m** of its whole 39 km is unexplained by `NSR`. ✅ **It independently confirms `P3-13`'s kind mapping** — `RM1040` "no stopping at any time" sits on `TIME_ZONE = 1` for **99.95%** of its length. `tools/kerbside_source_audit.py` runs the pipeline's own join over it and diffs: agreement **77.0% → 96.4%**, kind agreement **95.7% → 99.2%**, opposite-kerb **100 → 35 m**. ⚠️ **Codes 2/3/4 stay refused for a different reason than `Q54` gave** — the drawings paint those too (code 2 at 100%, code 4 at 90.2%); the codec cannot say *which class*, and that is 2,847 m. ✅ **28% more restriction made the mesh smaller** — 1,113 boundary stations not 1,179, 39,128 vertices not 39,176, because a code-5 run usually *continues* a code-1 run and removes the boundary between them. ⚠️ Swept the whole catalogue first: **`NSR` and `DTAD_RST_ZONE_LINE` are the only two datasets in HK open data that assert a kerbside stopping restriction** |
| `Q57` | **Why does "unsourceable" keep turning out to be wrong, and what else is published?** | Three records asserted no source for something the estate publishes, and the game is authored against all three | `DATA_SOURCES.md` | ✅ **Closed as a survey, 2026-08-20 — nothing built, nothing fetched.** Swept **3,810** DATA.GOV.HK packages (60 TD), **1,144** CSDI records, all **51** dTAD layers and all ~71 iB1000 layers, decoding domains from the publishers' own dictionaries. **The mechanism is one step, repeated**: a fact measured against Road Network v2 — the *semantic* dataset — then generalised to the estate, which is exactly what `Q54` and `Q56` were each corrected for. 🔴 **Four claims retired.** `Q53`'s "no marking data in any source" (arrows are **1,365** `DTAD_RD_MARK_SYM_PT` symbols with an `RM` code and a bearing; road text is **274** annotations); `Q19`'s "a carriageway width no source publishes" (iB1000 `CartoTransLine` `RM` = *"Road margin"*, **56,286** segments on sheets already downloaded, p50 **9.84 m** against a two-valued `width_m`); `hong_kong.yaml`'s "no dataset marks tram tracks" (`CartoTransLine` `TW` = *"Tramway"*); and "lane counts do not exist", which is literally true and concludes wrongly — the lane **lines** are published, so `lanes` is authored for want of a counter. ⚠️ **The width probe over-reads** at junction mouths and across dual carriageways — evidence a width exists, not a width to ship. ⚠️ **One trap recorded**: `DTAD_TW_STRIP_LINE` is tactile warning strips, not tramway, and matched *every* street in the region before the join gave it away. ✅ Genuinely absent, and now closed: a published lane-count attribute. ⚠️ Follow-on work is named and **unassigned** in `DECISIONS.md` |
**Closed:** `Q1` `Q2` `Q3` `Q4` `Q5` `Q7` `Q8` `Q9` `Q10` `Q11` `Q12` `Q16` `Q17` `Q18` `Q20` `Q23`
`Q25` `Q26` `Q27` `Q28` `Q29` `Q32` `Q33` `Q34` `Q34′` `Q36` `Q37` `Q40` `Q41` `Q43` `Q44` `Q45` `Q46` `Q53`. Each has a record in `DECISIONS.md`.

⚠️ **`Q26` closed on `C`, and `Q30`, `Q42`, `Q47` and `Q48` are all downstream of it** — every one
is consumed behind `survey_apply = 0.0` and draws nothing in the shipped build (`DECISIONS.md`
`P3-7a`).

---

## Risk register

| Risk | Severity | Mitigation |
|---|---|---|
| Novelty does not survive the first session | **Medium** | The honest reading of the `Q8` verdict — "gimmick" was the user's own word, and a gimmick carries one session. `P3-9a` asks this risk directly and earliest: a `B2` city with no fares, traffic or score is its harshest form, and **how long each driver keeps going before stopping is the number to write down**. Three levers, all built from assets already scheduled: the **losable style chain** (`P3-2b`); a **drivable roster**, made cheap by `P3-11`'s generator, since the same script builds the minibus, double-decker and tram; and **world-embedded challenges** pinned game-side to edge IDs. Only the first is in the slice; the others are named so Phase 5 does not reinvent them |
| Doesn't read as HK to locals | **High** | `P3-9` with ≥3 real drivers; run again every phase after |
| Carriageway occupied by solid geometry | Medium — ⚠️ **measured, and the routing half is now handled** | `Q19`, real since collision shipped. ✅ `tools/carriageway_occupancy.py` shipped 2026-08-18 and **fails**: 26 drivable level-0 edges keep less than one lane clear. ⚠️ **The fix this register assumed is not available** — narrowing would not clear them. ✅ **`Q51` closed the traffic exposure the same day**: the bundle publishes the width and `RoadGraph.is_routable` keeps AI off the edges it records as blocked, so this is no longer a blocker on `P3-3`. ✅ **The 21-against-26 gap is reconciled 2026-08-19** — plan cell size, and the split was 6 + 1 rather than five (`Q51`). ✅ **The reconciliation then found that `clearance.py` aliased walls at its 1 m along-edge spacing, and that is fixed the same day**: `ALONG_M` is `CELL_M` (0.5 m), the published set is **24 against the grader's 26**, and `e636` HARBOUR ROAD — the edge traffic *was* being routed onto — reads 0.00 m and is refused. The published width is a lower bound at the plan cell now, which it was not before. ⚠️ **What is left is the player**, who can still drive into an invisible wall on a road the graph calls legal. ✅ **The start line half closed as `Q52`, the same day**: `RoadSpawn.Pose` carries the clear width where the car is set down, and `verify_spawn.gd` fails on a bundle that starts the player in a wall — it reports and places anyway, so what refuses is the check, not the runtime. ⚠️ **It fires on nothing today** (`f_004` stands in 9.00 m of a 3.20 m lane, and no fare node lands on a blocked edge), so it is proven on four built start lines rather than on the city. Numbers in the `Q19` row above ✅ **2026-08-20 the grader names what blocks each edge and how far along it.** The building half is 1-3 m spot blockages on short junction stubs, not frontage, and traces to an **invented** carriageway width — 🔴 **not, as this register said until 2026-08-20, one no source publishes**: iB1000 `CartoTransLine` `RM` is the road margin, on sheets already downloaded, and it reads p50 **9.84 m** against the two figures shipped (`Q19`, `Q57`). ✅ **Measured properly 2026-08-20** by `tools/carriageway_margin.py`: the drawn ribbon crosses the published kerb at **75.0%** of 12,502 level-0 stations, p50 **+1.59 m**. 🔴 **And the register's new assumption is wrong too** — on the short edges this row calls the building half, overhang reads p50 **−0.36 m**, *inside* the kerb. The width is invented, the ribbon is too wide across the network, and neither fact predicts the edges that actually fail. Either the junction artefact survives or the building half is not a width defect; "short edge" and "junction" are the same population and cannot be told apart by exclusion (`Q19`). The interchange half is unchanged and Phase 4's |
| The vehicle shader unbinds and nothing says so | Low — ✅ **both binding paths held from 2026-08-18**, one residue left | `P3-11c` made the taxi a second consumer of `generated_scene_import.gd`'s material-name channel, and **every failure on that path is silent**: drop the name, the dictionary entry or the `.tres` and the body falls back to its `BaseMaterial3D` and renders very nearly as it did before. `check.sh` covered none of it — its seven generated-asset verify tools are all city assets, the eighth (`verify_beam_budget`) is the lamp *budget* rather than the lamp shading, and Godot exits `0` on a shader that fails to compile. `TestSurfaceMarkers` holds the ETL half (material name, exact-integer markers, the lens-vs-plate rule); the engine half is held by a render and a `grep -i "shader error"` on the driver log. `P3-11d` widened the same surface: `TestLampCircuits` holds the payload and `_check_wiring` refuses a renamed lamp in the generator, but nothing engine-side proves a circuit reaches the shader — an `instance uniform` that is never matched fails by staying at its default, which is every lamp out. ✅ **`tools/verify_vehicle.gd` shipped 2026-08-18 and closes both**, in `check.sh` **outside** the `VERIFY_GENERATED` gate — the taxi is a committed authored asset, so this is the second runtime contract CI checks on every push. Every check was proven by breaking the thing it guards; the six breaks and what each reports are in `DECISIONS.md` `verify_vehicle.gd`. ⚠️ **Residue, and it is the reason this is Low rather than closed**: nothing here compiles the shader. Headless has no rasteriser and Godot exits `0` on a shader error, so a syntax error in `vehicle_body.gdshader` still reaches a frame before anything says so — that half stays with a render and a `grep -i "shader error"` over the driver log |
| `P3-11e`'s night path cannot be exercised until a night rig exists | Low | The front lamps ship three triggers and the region exercises one. Shade and cover are proven under the HKCEC deck; **night is `read_rig` reading a rig `Q26` has not built**, so it is untested code that will stay untested — and its one authoring constraint (a night rig must *dim* its key light, never delete it, or the taxi reads "no rig" and drives dark) is recorded in `DECISIONS.md` `P3-11e` rather than enforced anywhere. Whoever builds that rig owes this a drive-test, and owes `sun_glint.gd` an `apply()` in the same breath |
| **The roster's headlamps cap at four cars, silently** | Low — ✅ **capped 2026-08-18, and the cap is checked** | Forward Mobile is not clustered: it pairs **8 spot lights per rendered object** and the fragment shader loops that fixed list — measured linear to 8, then **exactly zero from the 9th, no warning, no fallback**. `roads.glb` is **one mesh for the whole region** and on screen whenever the player is, so every beam in the game competes for the same 8 slots: at two lamps a car, **four cars**. Which four is pair order, not distance, so beams pop as the BVH re-pairs and the player's own car is not guaranteed a slot. `distance_fade` (35/15 m) ships as mitigation and genuinely frees slots (16 spots → 8 units of light; fading the first 8 gives the same 8 from the other half), byte-identical in the shipped frame — but it **bounds** the competitors rather than capping them. ✅ **The *nearest-N gets beams* rule this owed shipped 2026-08-18**: `BeamBudget` grants slots by distance to the active camera until `beams.tres`'s `max_spot_lights` runs out, so the ninth light is never asked for, and `verify_beam_budget.gd` holds it in `check.sh` — **outside** the `VERIFY_GENERATED` gate, so CI checks it without a built region. The shipped frame is byte-identical with the arbiter and without it. ⚠️ `distance_fade` stays; the two do different jobs. ⚠️ **Residual**: ranking is by camera distance alone, so a cinematic or look-back that moves the camera off the player would rank the player like any other car — `P3-3` should decide whether the player's slot is reserved. See `DECISIONS.md` `BeamBudget`. `P3-11e` |
| Grade separation is unreachable | Medium | `Q13`. Largely closed by `P2-7`'s sampling — median step 0.04 m — but the network is still *closed to driving* by `P2-2`'s refusal. Opening it is `P4-1` |
| Remaining `P3-7a` riders land invisible in the shipped build | Medium — ✅ **mitigated by not building them** | Raised to High when `Q26` closed on `C`, then answered the same day by closing `P3-7a` at what was judged and gating the remainder on `P3-9a`. ⚠️ The mitigation is the *parking*, not a fix — a resumed rider owes a criterion with teeth and is graded against `city_facade_elements.tres`, never against the default (`DECISIONS.md` `P3-7a`) |
| The survey's podium floors measure the treatment band, not the podium massing | Medium | `Q47`'s graded fail (−10.76 m systematic). Re-called 2026-08-11: data-only pack — the complement keeps the shader uniform (measured \|err\| p50 2.97 m on `P`-block towers, better than the corrected survey) and its true fill is owed by the ground-band batch's re-worded boundary question |
| **`facade_lab.json` is a committed input that no committed tool grades** | Medium — ⚠️ **realised once, and again 2026-08-20** | `Q37` was opened because the script that produced the shipped table was never committed; `tools/facade_survey.py` replaced it and *can* be re-run, but nothing **checks** the table it writes. `Q55` found 93 buildings measured off coloured placeholder with a scratch script, and no check in the repo would ever have noticed. ⚠️ `check.sh` cannot own this — it does not require a built region and should not start to — so the guard belongs in the survey tool with a test, beside the other hand-run graders. Every façade decision from `Q30` to `Q34′` to `Q45` reads this file |
| Perf misses 60fps on device floor | Medium | Budget defined up front; untextured merged tiles are the main lever; `P2-6` is a dedicated pass — and it needs `P0-3b`'s hardware |
| TAM too small to be commercial | Medium | City-agnostic ETL is the scaling answer — city packs, not one city. `Q8` strengthens this: if recognition is the product, a second city is a YAML file |
| GPLv3 forecloses the App Store | **Medium** | GPLv3 §6 conflicts with App Store terms, so store builds need a separate proprietary grant — available only while one party owns the whole copyright. Mitigated by `CONTRIBUTING.md` taking contributions **inbound MIT**. Zero exposure today (no outside contributors) and **no retrofit** once one declines, so the file must land before the repo goes public |
| Landmark depiction IP | Low | Untextured massing; legal sight-check before launch (Phase 7). **The top item in that brief** — the government data terms are permissive, so depiction is the one question left with a plausible adverse answer. See `LICENSING.md` |
| GDScript learning curve | Low | Small codebase; complexity lives in Python |

**Retired.** *Road data lacks Z values* (`Q1` — `ELEVATION` encodes the level; region holds). *Real
geometry isn't fun to drive* (`Q8` — replaced by the novelty risk above). *Source data quirks*
(`P1-3`/`P1-4` — both turned up and both were handled). *Building meshes blow the triangle budget*
(`P1-2`/`P2-1` — worst-case visible is 150,374 against 300k). *Terrain does not fit any budget* (224
of the 267 MB was the JPEG; `P3-10` ships none of it). *The player's car is two boxes* (`P3-11` —
24 → 1,168 triangles at the moment it retired, 1,180 today; generated from the chassis the scene
already published. The lesson it leaves is
that **the docs specified the car so fully that it read as done**, which is how a capability with no
owner hides).

---

## Metrics

Record measured values here, not estimates. ⚠️ **Bundle size is measured from a PCK, never summed
from source files** — that rule has been wrong in both directions once each (`Q16`).

| Metric | Target (mobile) | Latest | Date |
|---|---|---|---|
| FPS on device floor | 60 | — (no device yet — `P0-3b`) | — |
| FPS, Chrome on macOS, 2880×1450 | — | **119** (worst frame 9.7 ms) | 2026-07-31 |
| Draw calls | < 150 | **53** ✅ | 2026-08-01 |
| Visible triangles, worst measured | < 300k | **150,374** ✅ | 2026-08-01 |
| Resident triangles, worst measured | — | **≈380,700** — tile worst 280,783 (streamer verify, tiles only) + 99,877 of always-resident heroes, which sit outside the streamer since the `P3-6` amendment (280,807 all-in before it; 236,882 before `P3-10`'s ground). A ceiling, not a gate; `P2-6` measures the visible figure under HKCEC | 2026-08-12 |
| Texture memory | < 128 MB | **0** — no textures ship, ground included | 2026-08-05 |
| Bundle size | < 200 MB | **39.17 MiB** PCK (41,069,488 bytes, re-exported 2026-08-20 on `Q56`'s `painted_vehicle_types: [1, 5]` — **+5,792 B for 28% more kerbside restriction**, because admitting code 5 *removes* run boundaries more often than it adds them). Prior: **39.16 MiB** (41,063,696 bytes, 2026-08-19 on `P3-13` and its kerb-rail thinning; 38.85 MiB / 40,744,128 B on `P3-12` alone). **+41,344 B is the markings payload**, measured from two exports with one variable changed (40,702,784 before) against **279,532 B** of raw VEC2 across 34,924 vertices — the pack compresses it **86%**, because `TEXCOORD_1.x` is a per-edge constant with 17 distinct values region-wide. ✅ **The over-the-wire figure was re-measured 2026-08-20 on this same export: 41,069,488 B PCK + 38,817,643 B wasm = 79,887,131 B = 76.19 MiB**, up **+0.45 MiB** on the 75.74 MiB below — which is `P3-12`'s markings, `P3-13`'s kerbside and `Q56` together, since that prior figure was measured on the pre-markings 18 Aug cut. The itch zip of the same nine files is **49,731,639 B (47.43 MiB)**. It is what a tester on mobile data pays and what that round is bounded by, not the PCK alone. Prior: **38.72 MiB** PCK (40,601,608 bytes, the `P3-9a` web export), + **37.02 MiB** wasm — **75.74 MiB over the wire**, which is the figure a tester on mobile data pays and the one `P3-9a` is bounded by, not the PCK alone. **+0.05** over the 2026-08-12 figure below is the whole of `P3-11c`/`d`/`e` — a shader, `vehicle_lamps.gd`, +12 triangles and two shadowless `SpotLight3D`s. Prior: **38.67 MB** PCK (**+4.82 MB** is the `P3-6` amendment: the mesh-sourced HKCEC ships 99,577 sliced-and-repainted triangles as generated data, replacing the 213 KB authored model; 33.85 before it; 36.57 before `P3-6` — the two excluded source buildings gave back **−2.72 MB** and the authored heroes cost 17 KB; 36.32 before the `TEXCOORD_1` payload; **32.30** before `P3-7`'s `TEXCOORD_0`; **27.73 immediately before `P3-10`**, so the **+4.56 MB was the ground and its collider** — only the total was measured, not the split) | 2026-08-20 |
| Tile triangles, LOD0 / LOD1 | — | **521,693 / 253,097** (434,149 / 222,375 before the ground) | 2026-08-05 |
| Ground the source has and the bundle does not | — | **0.76%** of the region (1.76% before `Q25`); **0.42%** within 2 m of a tile boundary against 0.54% in the interior | 2026-08-05 |
| Ground standing proud of the carriageway | — | **2.2% of area, 1.00% of sampled points.** ⚠️ Not comparable with the 1.9% / 0.49% recorded before `Q25`: closing tears made 3.35% more of the carriageway *measurable*. Like-for-like over what both bundles could see, **2.181% → 2.205%** | 2026-08-05 |
| Buildings stage, peak RSS | — | **657 MB** (924 MB before the ground was clipped to the region) | 2026-08-05 |
| Road surface triangles | — | **32,233** at 39,128 vertices, from 1,113 restriction-boundary stations (**32,295 / 39,176 / 1,179 before `Q56`** — more restriction, fewer stations; 37,259 / 44,142 before `P3-13`'s kerb rails were thinned; 28,170 before `P3-13`; 25,028 before `Q24`'s stations; 35,039 before the kerb fix) | 2026-08-20 |
| Boot to drivable (web, warm) | — | 830 ms, of which 260 ms is tile instantiation | 2026-07-31 |
| Tab memory (web) | — | 307 MB | 2026-07-31 |
| Junction cap overlap onto the arm beneath it, per trimmed end | reported — it sizes the markings fade | p50 **0.00 m**, p90 **1.17**, p95 **2.36**, p99 **3.62**, worst **4.21 m**; **203 of 1,398** ends overlap at all (14.5%), reproducing `ARCHITECTURE.md`'s published 210 from a different direction. Derived from the published `trim_m` per (node, level) group — an approximation of the hull's own reach, which is why the shipped 6 m fade keeps 42% margin. ⚠️ It also prices the *other* direction: an edge shorter than twice the fade never carries a full marking, and drawn length is p10 **4.0 m** / p25 **12.5 m**, so 9 m left 169 of 797 edges bare against 6 m's **121** (`Q53`) | 2026-08-19 |
| ETL full-run time | — | **13.7 s**, whole region from a warm source cache, nine stages. **4.4 s of it is the `clearance` stage** (`Q51`) — the second-longest after `buildings`, and the price of publishing a number the game cannot derive. Priors: **12.8 s** with clearance at 3.8 s and `ALONG_M = 1.0` (2026-08-18), and **3.0 s** across seven stages (2026-08-02) | 2026-08-19 |
| iB1000 topography fetch, Wan Chai | — | **260.0 MB**, 6 sheets, first run; re-run all cached. In-pipeline read reproduces `Q47`'s counts exactly (1,595 blocks, 280 `P`, 100% `T`/`P` level fill) | 2026-08-10 |
| Tower↔block join, Wan Chai | — | `podiums` stage, **2.1 s**, byte-stable reruns. 1,595 pieces → 1,480 logical blocks (104 cross-cut groups); 458/1,134 towers meet a `P` block, 228 exact meets; **310/1,385 stems** carry a data boundary (291 certain, p50 13.6 m) | 2026-08-11 |
| Drift, held full lock from 63.0 km/h (skidpad) | slides, does not spin | **21.8° peak slip, 1.25 m/s², 46.4 m** at `drift_rear_grip_scale = 0.66` on `VehicleBody3D` (`Q50`, 2026-08-18). ⚠️ Slides more and scrubs less than the raycast car's **16.0° at 2.39** — but there is **no value at the 14° target**, the window either side being 0.02 wide, and a 0.5 s tap now returns **1.9°**, i.e. nothing. Superseded figures: 16.0° at 2.39 over 36.9 m at `handbrake_lock = 0.15` (`Q49`, 2026-08-17) | 2026-08-18 |
| Handbrake tap, 0.5 s at 62.8 km/h (skidpad) | recovers | **7.1° peak slip, 0.18 m/s²** — the car breaks loose and gathers itself up, costing 2.6 km/h over 4 s. ⚠️ At full lock the same tap reaches **170.5°**: the spin is not an artefact of holding the button | 2026-08-17 |
| Power-on corner, 4 s full lock (skidpad) | — | **63.02 → 62.35 km/h**, yaw **−358.2°**, 70.0 m on `VehicleBody3D` at `tyre_grip = 2.5` (`Q50`). 🔴 The speed is held only because the grip was seeded to hold it: the car turns **16% less** than the raycast model's −428.9°, and with one isotropic budget a power-on corner is free again — which is exactly what `Q49`'s ellipse existed to stop. Superseded: 62.78 → 62.26, yaw −428.9°, 68.2 m (`Q49`) | 2026-08-18 |
| Carriageway occupancy, narrowest clear corridor per level-0 edge | ≥ 3.20 m (one lane) | 🔴 **26 of 737 edges below it**; worst **0.00 m** (`e233`, WAN CHAI INTERCHANGE), then 0.48/0.49 m on `e125`, `e314` LEIGHTON ROAD, `e546` CONVENTION AVENUE, `e788` HUNG HING ROAD FLYOVER, `e485`. ⚠️ Six of the eight tightest are equally blocked inside the **un-widened** width, so this is not the 1.6× widening. ✅ **All 26 are listed since 2026-08-20**, with the blocker and the starved extent: the 14 `BUILDING` failures starve **1-3 m** each, `e233` **42 m unbroken** (`Q19`). 47,275 stations judged, **129** too trimmed to judge (this row published 122, which no run reproduces), 99.8% of 1,115,582 cells measured. Run time **26.1 s** (47.4 s before the carriageway walk was unified — the second walk was 22 s, and duplicating it also made the occupier prune's superset property a convention rather than a guarantee). ⚠️ **This count is very nearly a function of one constant**: `26 / 18 / 9` at a `--index-cell-m` of 1.00 / 0.50 / 0.25, because a plan cell blocks in full on one sample (`Q51`) | 2026-08-20 |
| Carriageway occupancy, share of all drawn carriageway at bumper height | `BUILDING` ≤ 1.72%, `INFRA` ≤ 1.60% | ✅ Both pass — `BUILDING`+`LANDMARK` **1.302%**, `INFRASTRUCTURE` **1.115%** at grade, off-grade **1.278%** (reported, never gated), total **3.693%**. ⚠️ Against `Q19`'s hand-measured **5.17%**, and 🟡 **not reconciled** — same direction and rough ratio on all three, most likely a different population after `P3-6` moved HKCEC out of the tiles. Landmarks counted separately (0.085%) so that difference cannot hide | 2026-08-18 |
| Widening sweep, edges under one lane at each factor | narrowing must clear the building half | 🔴 **It does not, and re-sweeping on `Q51`'s corrected population hardened it.** 24 edges at the shipped 1.60x and **no edge clears at any factor down to 1.30x**; 26 from 1.45x down, because `e595` THOMSON ROAD and `e207` CANAL ROAD EAST are starved by the clipping rather than by anything that moved. Class split 9 `INFRASTRUCTURE` / 14 `BUILDING` / 1 `LANDMARK`. Against the car's own 1.8 m rather than one lane: **19 of 24**. `tools/narrowing.py`, 30 s. ⚠️ Columns are worth ±0.25 m — the samples re-phase as the corridor narrows. ⚠️ **This tool imports `pipeline.clearance` whole, so `ALONG_M` moves its population without any widening changing** — prior, at 1 m spacing: 21 / 8 / 12 / 1, one edge lost, 17 of 21 under the car | 2026-08-19 |
| Carriageway clearance published by the pipeline, per level-0 edge | reported, not gated | **24 of 737 edges** under one lane (3.20 m); **five at 0.00 m** — `e125`, `e233` (WAN CHAI INTERCHANGE), `e627` (GREAT GEORGE STREET), `e636` (HARBOUR ROAD), `e740` — then 0.25 m on `e314` and 0.50 m on `e335` and `e485`. 77,286 cross-sections judged at the shipped **0.50 m** spacing, 13,317 left to their junction caps; 120,957 occupier triangles reach the corridor from 66 tiles and 2 heroes. Run time **4.4 s**, peak RSS **789 MB**. ⚠️ **Against the grader's 26 on the same bundle** — ✅ reconciled 2026-08-19 as plan cell size, per-edge table and brute-force verification in `DECISIONS.md` `Q51`, ratcheted by `tools/clearance_reconcile.py`, now at **24 / 26 / 4 disagreements**. ✅ **The published width is a lower bound at `CELL_M`** since `ALONG_M` dropped to it 2026-08-19 — it was **not** one at the 1 m spacing shipped before, which read 21. ⚠️ It is a bound at `CELL_M` and no finer: a 0.25 m walk reads **25**, and was refused at **1.088 GB** peak RSS, 2.1× the shipped run and back toward the 1.64 GB the piece budget was introduced to cut. **9,779** of the 120,957 triangles could not be split to 0.50 m and block by a box at full height range. ⚠️ The triangle count rose with the spacing (118,482 → 120,957) because the prune is corridor-shaped, not because the city changed. *Prior, at `ALONG_M = 1.0` (2026-08-18): 21 edges, 38,664 cross-sections, 118,482 triangles, 9,727 clipped, 3.8 s and 482 MB — itself down from 12.2 s before the corridor-shaped prune, 6.6 s before `einsum` was routed through BLAS and the barycentric lattices cached, and 1.64 GB before the piece budget; none of those three changed a published number* | 2026-08-19 |
| Drawn carriageway against the edge two publishers print, per level-0 station | reported, never gated | **12,502 stations on 709 of 737 level-0 edges, 92.3% coverage** (`tools/carriageway_margin.py`, `Q57` follow-on 2). Near-side overhang — drawn half-width minus the nearest published edge — p50 **+1.59 m**, p75 +2.41, p90 **+3.24**; **75.0%** of stations sit past the published kerb and **63.0%** past it by over a metre. Sources: TD `RM1108`/`RM1109` (745 segments) preferred, iB1000 `RM` (29,018) behind it; both answer 1,429 stations and disagree by p50 1.13 m. ✅ **Stable against its own ray cap** — p50 1.69 / 1.59 / 1.53 / 1.50 m at 10 / 15 / 20 / 25 m, coverage 87.7% → 95.0% — unlike the occupancy count above, which is nearly a function of one constant. ⚠️ The two-source *agreement* is cap-sensitive where the headline is not (p90 3.76 m → 14.30 m over the same sweep); quote the headline. 🔴 **Edges under 20 m read p50 −0.36 m**, inside the kerb, against +1.66 m on edges over 47.3 m — the width story is weakest exactly where `Q19`'s building half fails (`Q19`) | 2026-08-20 |
| Kerbside restriction published, and how far a second source agrees | reported, not gated | **33,385 m over 722 edge sides** (19,764 double / 13,621 single) from 530 of 579 `NSR` features. Against Traffic Aids Drawings v2 through the same join: **both sources 32,410 m — 96.4% of the union**, published-only 975 m, drawing-only 220 m; **kind agreed 99.2%** of the metres both carry, opposite-kerb **35 m**. ⚠️ Before `Q56` admitted `VEHICLE_TYPE = 5`: 26,065 m / 650 sides, **77.0%** agreement, **7,101 m** the drawing carried and the bundle did not, kind agreement 95.7%. `tools/kerbside_source_audit.py` | 2026-08-20 |
| Kerbside yellow painted on the shipped mesh vs the runs published | reported | **22,137 m painted, 22,451 m reachable — 3% gross** (over 230 m, missed 545 m); a line on every kerb would over-paint 34,377 m, **153%**. ⚠️ **3,219 m of real restriction is on a kerb the widening paved over** and no shader change reaches it (2,909 m before `Q56`). 9 of 14 taxi stands stand inside a genuine restriction. `tools/kerbside_error.py` | 2026-08-20 |
| Deck error, \|error\| p90 vs shipped tiles | ≤ 0.50 m | **0.095 m** ✅ | 2026-08-02 |
| Taxi triangles in scene | 800–2,000 | **1,180** (604 body, 4 × 144 tyres) — `P3-11c` added a shader and moved no geometry; `P3-11d` added **12**, the high-level brake lamp | 2026-08-17 |
| Braking falloff, 65 → 4 km/h (skidpad) | flat curve | ⚠️ **Figures below are the raycast car's and were not re-measured for `Q50`** — `brake_force` is a different quantity now and re-seeded 2400 → 40, and the whole-stop figure did survive (8.75 against 8.79 m/s²), but the falloff *curve* is unmeasured on `VehicleBody3D`. **−17%** (9.65 → 8.06 m/s²) at `brake_force = 2400`, from **−36%** (4.78 → 3.07) at 900 — low-speed braking **2.6× stronger** and less than half as speed-dependent (`P0-5b/c/d`). The falloff is Godot's viscous `default_linear_damp`; raising the constant term is the only lever against it. Stop from 72.8 km/h in 2.30 s / 21.9 m. Grip never binds: `grip_longitudinal` capped each wheel at ~9.4 kN at rest, ~8.1 kN on the unloaded rear axle under 0.8 g, against 2.4 kN asked — that dial is gone with `Q50`, and `tyre_grip` has not been checked against the same question | 2026-08-17 |
| Coast-down to rest, flat skidpad | reaches 0 | **6.5 s** from 31 km/h, **1.5 s** from 5 km/h ✅ — was **never**: 30.6 km/h was still rolling at 3.9 after 13.8 s, the coast force being purely viscous (`P0-5b/c/d`, fifth bug). Fixed by `rolling_resistance_mps2 = 0.8`. Lift-off drag at 60 km/h rises 2.50 → 3.30 m/s² (**+32%**); throttle-on acceleration byte-identical | 2026-08-17 |
| ⚠️ Coast decay with both profile dials forced to zero | — | **0.100/s** — Godot's `default_linear_damp` 0.1, unoverridden in `project.godot`, damps the body underneath the controller; restoring `coast_drag_per_s = 0.05` gives 0.150/s, so the dial is honest but is only **one third** of the viscous total. Tune it against telemetry, never against itself | 2026-08-17 |
| ⚠️ Braking on the HKCEC down-ramp | sheds speed | **~20 km/h/s** ✅ — at 900 N the taxi *gained* speed under full braking (38.90 → 40.65 km/h): `gravity_scale` 1.6 makes the slope pull harder than 0.31 g could resist | 2026-08-17 |
| 🔴 Handling figures first published from `city_drive.tscn` | — | **Withdrawn and re-measured on `skidpad.tscn`.** A 0.14° micro-gradient is worth ~0.05/s at 4 km/h — the whole quantity under test — so the first ablation did not compose (0.089 + 0.05 ≠ 0.108) and, worse, flattened the 36% brake falloff into an apparent 12.4-against-11.2, on which the user was wrongly told their report was mistaken. Drag and brake coefficients belong on the skidpad, which says so in its own header | 2026-08-17 |
| Uniform gloss on the taxi (`roughness 0.9 → 0.25`), probe | — | 🔥 **Refused.** Red bodywork `C*` **79.06 → 70.08**, hue **−7.9°** at `taxi` t04.50 (shade: 57.47 → 53.15, −14.5°) while `L*` *rose* — albedo-independent light, `Q27`'s signature. Glazing gained `L*` +1.17 / `C*` +3.77. The split that made `P3-11c` a shader rather than a material value | 2026-08-16 |
| `P3-11d` lamp emission, the high-level brake lens | — | Same 294 px, braking at `t03.30`: ablated `L* 2.29 / C* 9.35` · 1.2 → **67.04 / 56.44** · **1.6 shipped → 72.72 / 44.34** · 2.3 → 79.17 / 31.81. ⚠️ The red channel is clipped at 255 from 1.2 up, so above it the dial buys bloom and spends **redness only** — 2.3 is the white tail lamp `ART_DESIGN.md` has refused twice. `L* 2.29 → 72.72` is the feature: black when the circuit is out | 2026-08-17 |
| `P3-11e` front lamps, lit under cover vs unlit in sun | — | Front cluster box under the HKCEC deck (`lamps_nose` t01.20): **peak L\* 100.00**, clipped — emissive and over the bloom threshold — against a **38.74** mean for the box, so the lamp stands ~61 L\* clear of its surround. The same lenses parked in open sun (`lamps_parked` t08.50) **peak 96.71** and never clip: a cream lens catching the sun, which is what an unlit lamp is. ⚠️ **Not an ablation and not aligned frames** — the car has moved between the two, so this is two states of one lamp, not a pixel diff. A stationary before/after was attempted and abandoned: the streamer is still instancing tiles across the `dark_hold_s` window, so 41% of the frame changes for reasons that are not the lamps | 2026-08-17 |
| `P3-11c` gloss dial on the red paint, `C*` at `taxi` t04.50 | — | at `fresnel_power` 4.0: matte 79.06 · `reflect 0.12`/`rough 0.9` **73.65** · `0.12`/`0.55` **69.86**. Shipped is **71.63** at `fresnel_power` 6.5 (cost −9.20 → **−7.43**) · uniform `rough 0.25` probe 70.08. Linear in what is asked for, and **9× the frame median even at the bottom** — the "only chromatic object" property survives the whole cost. ⚠️ `P3-9a` recognition is the risk this dial answers first | 2026-08-16 |
| `P3-11c` fresnel, ablated vs tightened | — | 🔴 **Ablated** (forced to 1.0, reflection at every angle) the paint loses a *further* `C*` **13.75** (69.86 → 56.11) — the term is what makes the clearcoat affordable, not decoration. ✅ **Tightened** `fresnel_power` 4.0 → 6.5 refunds `C*` **+1.77** and takes hue rotation −7.4° → −6.3° for **−0.06 `L*`** of glazing | 2026-08-17 |
| `P3-11c` sun glint, glazing `L*` by heading (skidpad) | — | **+0.01 / +2.48 / +6.03** at three headings — absent at one, strong at another, which is what separates a glint from a wash. 🔥 `SPECULAR 0.5→0.9` refused first: mean `L*` +5.8 but p99 heading spread only 2.69→3.54, plus `C*` −3.08 on the paint, because `SPECULAR` scales *ambient* specular and with no probes the ambient half dominates. ⚠️ On flat geometry a glint is per-facet — a pane flashes whole rather than a spot sliding across it | 2026-08-17 |
| `P3-11c` glazing structure, `L*` sd within the glass | — | **0.05 → 6.35** at t01.20 (range 0.1–0.3 → 0.2–22.2), **1.25 → 7.69** at t04.50. The sky *gradient*, not its strength — a flat reflection colour and the gradient have similar means and look nothing alike, which is why the first two tunings both read wrong | 2026-08-16 |
| `P3-11c` shipped, vs the pre-shader build | paint unmoved | red paint `L*` +0.01 / `C*` **−0.02** (t01.20), −0.55 / **−0.62** (t04.50) — **7%** of the probe's paint cost; glazing `L*` 0.13 → 3.05 and 0.52 → 3.62; silver trim `C*` 10.00 → **6.24**; **0 px moved outside the car box** at either camera. Lamp lenses `L*` +0.77 / +0.41 — real, and not enough for the red tail lens | 2026-08-16 |
| Façade grammar reader, strict-pool agreement | ≥ 16 / 20 | **19 / 20** ✅ — `Q41`'s gate: marginal 6/6, refusal 14/14, glazed 23/24, every bar fixed before the run. `claude-opus-5`, prompt `61fae4b11722bb5c` | 2026-08-08 |
| Façade grammar reader, resurvey on the shipped model | ≥ 16 / 20 | **18 / 20** ✅ — same gate, same labels: marginal 6/6, refusal 14/14, glazed **24/24**. `claude-sonnet-5`, prompt `d6e59b39307b215d`; Haiku 4.5 failed (15/20, refusal 12/14) | 2026-08-09 |
| Full-region façade survey, faces read | — | **4,734 / 8,704 (54%)**, 1,769 / 2,214 buildings ≥ 1 read face; 8,614 batch reads, 0 errors, ~$21 (vs ~$103 sync Opus estimate) | 2026-08-09 |
| Region glazing survey (dip + tint tables) | — | **2,171 buildings**, six sheets, local compute only; dark-bluer-than-light 89%, median shift −6.08 `b*` | 2026-08-09 |
| Façade survey rows measured off **coloured** placeholder | zero rows built on synthetic panels | 🔴 **97 atlases on 93 of 2,213 buildings** (4.2%), 92 clearing `vegetation_max`. Rejecting the panels moves **43 past `Q33`'s 0.46 `Δab`**, 15 past 2.0, 5 past 5.0; Δ`L*` median **+0.00**, range **−33.22 … +54.69**. Wall-texel share p50 **0.94%**, p90 33.0%, max **96.5%**. ✅ The grey half is caught — 2,429 of 3,203 `B` atlases, 1,982 at `#3c3c3c`. ⚠️ **A floor**: detection needed one exact colour at ≥20% of an atlas on a 1-in-16 lattice, and the deltas reject only the colours found. Scratch measurement — **no committed tool reproduces it** (`Q55`) | 2026-08-20 |
| Glazing-gate calibration, dip vs reader | usable J | 🔥 **best Youden 0.100**, sign-unstable across sheets, blank-vs-windowed inverted — dip gate killed, glazing is the reader's call (`Q40`) | 2026-08-09 |
| `TEXCOORD_1` survey payload, PCK cost | ~2 MB estimated (`Q40`) | **+0.24 MB** (36.32 → 36.57 MB, one variable changed) — 7.50 MB raw VEC2, 97% pack compression on a per-building-constant channel | 2026-08-09 |
| Fenestration gate, walls forced solid by the survey | — | **66.2% → 0.2%** of 346,656 lod0 wall vertices (`Q43`); `punched` is 50.1% of them and was 100% zeroed. ⚠️ `Q26`'s first figures (59.7%/39.3%) are on a denominator of *all* 639,834 lod0 vertices, ground included | 2026-08-09 |
| Candidate `A″` (`Q43` gate + survey + fabric), responds / p90 \|d`L*`\| | — | `street` **19.7% / 32.43** · `skyline` **11.6% / 9.47** · `kerb` **14.8% / 36.79**, against `A′`'s 14.5/13.37 · 10.3/6.75 · 8.8/22.37. 12 runs, every `r1`/`r2` byte-identical, all 12 parked `C` frames byte-identical to `q26b` | 2026-08-09 |
| Candidate `A″`, whole-frame mean `C*` vs `C` | ≤ `A` | `street` **+1.40** (`A` +2.28) · `kerb` **+0.57** (`A` +1.43) · `skyline` **−0.06** (`A` −0.02) — 39% and 60% of `A`'s chroma cost given back, and not by deleting glass (`Q30`) | 2026-08-09 |
| Candidate `A⁗` (`Q46` quiet tier over `A‴`), responds vs `A‴` | — | `street` **1.6% / p90 36.52** · `skyline` **1.8% / 8.28** · `kerb` **0.9% / 23.15** — the response is confined to refused stock, and its `C*` p90 falls 28.1 → 22.6 (`street`), 27.6 → 19.3 (`kerb`). Siblings byte-identical; parked `C` byte-identical to `q26_C_cf19201` with the quiet values authored. Frames `build/driver/q26_A4_ab98183/` | 2026-08-10 |
| Candidate `A⁗`, whole-frame mean `C*` vs `C` | ≤ `A` | `street` **+1.10** (`A` +2.28) · `kerb` **+0.74** (`A` +1.43) · `skyline` **−0.04** (`A` −0.02) — below `A‴`'s +1.16/+0.78 on both street-level cameras | 2026-08-10 |
| Warm glass (`b*` > +3) among reader-glazed | — | **6%** region-wide (14/226); `9D` decomposes 17/56 → 7/51 decontaminated → 0/15 conditioned — no palette extension | 2026-08-09 |
